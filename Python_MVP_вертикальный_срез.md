# Python-режим · Вертикальный срез MVP

Не концепт. **Технический бриф на разработку.** Один мир, 10 уровней, полная полировка, готовое к тестированию на 20-30 школьниках.

---

## 0. Что это и почему именно так

Эксперт сказал: **не строй 3 мира. Сделай 1 идеальный. Дай школьникам. Смотри где улыбаются, где скучают, где путаются.** Только потом масштабируй.

Этот документ — **план вертикального среза**: минимальная, но полностью работающая версия ядра игры.

Всё что нужно для теста ядра:
- **1 полностью отполированный мир** (10 уровней)
- **Движок «Живой Python»** (только нужные объекты)
- **Профик** с 10 репликами и памятью
- **Карта** с 15 точками (14 закрыты)
- **1 карточка Осколка** и её раскрытие
- **1 «оживление» мира** после прохождения
- **Одна способность Профика** после мира
- **Звук** (5-7 ключевых событий)
- **Кнопка «▶ показать»** — визуализация выполнения кода
- **XP интеграция** с Ареной

Если после этого школьники говорят «дай ещё» — масштабируем на 15 миров. Если нет — знаем, где чинить, ещё не потратив 200 часов.

---

## 1. Правило проекта (переносим из v3)

> **Игрок не должен чувствовать, что учится.**
> **Он должен чувствовать, что проходит приключение,**
> **где программирование — инструмент решения задач.**

Плюс новое правило от эксперта:

> **Визуализируем только то, что реально становится понятнее.**

Список / поезд — да, становится понятнее.
Lambda / zip / map — нет, только цирк. Такое показываем текстом.

---

## 2. Выбор мира для среза

Не Мир 1 (Порт) — там слишком простой Python.
Не Мир 5+ (нужны предыдущие).

**Берём Мир 3 «Дюны Возврата» — циклы `for` и `range`.**

Почему:
- Самая яркая тема для визуализации («Живой Python» здесь работает наглядно — дорожка из плиток по которой прыгает Профик).
- Уже даёт wow-эффект (не «print работает», а «Профик прыгает по моим командам»).
- Циклы — центральная тема ЕГЭ, если сработают — почти всё остальное сработает.
- Механика «конвейер» универсальна.

**Но чтобы не потерять первое впечатление** — MVP включает **50-секундный пролог** (одна сцена с print) прямо перед входом в Мир 3.

---

## 3. Object Model «Живого Python»

То, чего не хватало в v3. Для разработчика.

### 3.1. Базовый класс объекта

```typescript
interface PyObject {
  id: string;              // уникальный id для анимаций
  type: 'variable' | 'int' | 'str' | 'list' | 'range' | 'bool';
  value: any;              // текущее значение
  displayName?: string;    // подпись на «коробке»
  x: number; y: number;    // позиция на canvas
  animation?: Anim;        // текущая анимация
}
```

### 3.2. Конкретные типы для MVP среза (только нужные)

```typescript
// Переменная — коробка с надписью
interface VariableBox extends PyObject {
  type: 'variable';
  value: PyObject;         // что внутри
  glow: boolean;           // подсвечена сейчас?
}

// Range — дорожка из плиток
interface RangePath extends PyObject {
  type: 'range';
  start: number;
  stop: number;
  step: number;
  tiles: TileSprite[];     // визуальные плитки
}

// Цикл for — контроллер, который двигает Профика по range
interface ForLoop extends PyObject {
  type: 'for';
  iterVar: VariableBox;    // куда кладётся текущее значение
  iterable: RangePath;
  bodyCode: PyStmt[];      // что делать на каждом шаге
  currentStep: number;     // на каком шаге сейчас
}
```

### 3.3. События анимации

Каждое действие Python = событие + анимация + звук + хаптик.

```typescript
type Anim = {
  type: 'boxOpen' | 'boxFill' | 'profikStep' | 'profikHop' | 'tileHighlight' | 'trainAdd' | 'trainRemove' | 'errorShake';
  duration: number;        // мс
  ease?: string;
  sfx?: string;            // ключ звука
  haptic?: 'light' | 'medium' | 'success' | 'error';
}
```

### 3.4. Рендер

Один `<canvas>` 360×640 (портрет). Ремённый лупер: 60fps в активной сцене, 0fps в паузе.

Простой rendering pipeline:
1. Фон (background мира — SVG через drawImage)
2. Статические объекты (плитки, коробки, поезд)
3. Профик (SVG-спрайт, 4 состояния: idle, thinking, hop, cheer)
4. Активные анимации
5. UI слой (кнопки, текст) — отдельный DOM поверх canvas

Никаких 3D. Никаких физических движков. Всё через `requestAnimationFrame` + `easings.net`.

### 3.5. Звук

Web Audio API. Пресеты (all ≤ 200мс):

| Событие | Звук | Хаптик |
|---|---|---|
| Коробка открылась | «тук» | light |
| В коробку положили значение | «плюх» | light |
| Профик прыгнул на плитку | «топ» | light |
| Правильный ответ | «дзынь» | success |
| Ошибка | «пффф» | error |
| Раскрытие Осколка | «фанфара 1.5с» | success |
| Оживление мира (порт → чайки, дюны → ветер) | окружение (loop) | — |

Все звуки — короткие WAV/OGG до 15КБ. Loop-ы — до 40КБ.

---

## 4. 10 уровней Мира 3 «Дюны Возврата»

Каждый уровень 30-90 секунд. Ощущение: Профик пересекает пустыню, шаг за шагом.

| # | Название | Механика | Что учит |
|---|---|---|---|
| 1 | Первый шаг | Тап `range(5)` → Профик прыгает 5 раз | `for i in range(n)` |
| 2 | Правая граница | Ловушка: сколько прыжков в `range(1,10)`? | правая граница не входит |
| 3 | С шагом | Собери `range(?,?,?)` чтобы получить 2,4,6,8,10 | шаг |
| 4 | Обратный ход | Профик должен спуститься с бархана | отрицательный шаг |
| 5 | По списку | `for x in [10,20,30]`: Профик подбирает камни | for x in list |
| 6 | По строке | `for c in "ЕГЭ"` — 3 буквы-плитки | строка тоже итерируется |
| 7 | Значение vs индекс | Ловушка: `for x in [10,20,30]` vs `for i in range(len(a))` | важное различие |
| 8 | «Мираж» (мини-босс) | Half кода занесён песком. Догадайся что было | комбо всех приёмов |
| 9 | Задача 25 (мини-версия) | Перебор чисел + вывод кратных 7 | приход к ЕГЭ |
| 10 | Финал: «Оазис» | Собери большую программу с 3 разными циклами | закрепление |

**После уровня 10** — раскрытие Осколка «Песочные Часы» + оживление Дюн + новая способность Профика «умеет бегать».

---

## 5. Кнопка «▶ показать» (по совету эксперта)

Одна из главных находок. После того как игрок собрал код:

1. На экране код (например, `for i in range(5): profik.hop()`).
2. Кнопка **▶ показать**.
3. Тап — на экране Профик выполняет ровно то что написано. 5 прыжков по 5 плиткам. С звуком «топ, топ, топ, топ, топ».
4. Готово — «дзынь», Профик машет лапкой.

Это делает связь «код → мир» осязаемой.

**Технически:** цикл проходит по `bodyCode` в `ForLoop`, каждая инструкция — event → animation.

---

## 6. Способности Профика (замена «инструментов»)

По совету эксперта — награда игровая, не функциональная.

MVP среза даёт **одну способность** после Мира 3:

**«Умеет бегать»** — на всех дальнейших уровнях Профик может выбирать: идти шагом или бегом. Бегом = быстрее анимация, лёгкая иконка ⚡ рядом. Только косметика, но приятная.

В полной игре — способности после каждого мира:
- После Мира 5 (строки) — тащить несколько букв за раз
- После Мира 8 (списки) — тащить несколько предметов
- После Мира 13 (функции) — телепортироваться
- После Мира 15 — призывать своих клонов

Но это в далёком будущем. В срезе — только «бегать».

---

## 7. Профик с памятью (в срезе — минимум)

10 базовых реплик:

1. Вход первый раз: «Ты пришёл. Я знал.»
2. Вход после перерыва: «Ты вернулся. Я скучал.»
3. Ошибся 3-й раз: «Погоди-погоди. Дай покажу трюк.»
4. Быстро отвечает: «Опять быстро? Ну попробуй и этот с первой.»
5. Долго думает: «Не торопись. Думай спокойно.»
6. Играет ночью (>22:00): «Опять ночью программируешь? Ладно, я тоже кот.»
7. Идеальное прохождение: «Ух. Ты машина.»
8. Прошёл мини-босса: «Ага! Так и знал что справишься.»
9. Финал мира: «Ты его сделал. Смотри что получилось.»
10. Возврат на карту: «Слышишь? Дюны шумят. Это ты их разбудил.»

Хранение состояния — в CloudStorage (уже есть в Арене):

```json
{
  "python.profik_memory": {
    "avgAnswerMs": 3200,
    "errorsInSession": 1,
    "lastPlayHour": 23,
    "lastPlayDate": "2026-08-15",
    "perfectRun": true
  }
}
```

---

## 8. Финал среза («репетиция» финала игры)

После прохождения Мира 3:
1. Раскрытие Осколка «Песочные Часы» (3D-карточка, звук фанфары).
2. Карта: Дюны оживают (проступают следы, кактусы цветут, слышен ветер).
3. Профик появляется рядом: «Умею теперь бегать. Показать?» → тап → короткая пробежка через дюны.
4. Появляется следующая точка «Пещеры Ожидания» — но она **закрыта** (нужно ждать следующей итерации).
5. Заглушка: «Пещеры откроются в следующем обновлении. Ты крут.»

Игрок не расстраивается — понимает, что это MVP-версия.

---

## 9. Реалистичный тайминг (переоценка)

Эксперт сказал: 60 часов — недооценка, будет 150. Он прав. Полировка съедает всё.

Разбивка среза:

| Компонент | Часы |
|---|---|
| Object Model + рендер-движок | 25 |
| Уровни (10 штук × 3 часа среднего) | 30 |
| Звуки (запись + интеграция) | 8 |
| SVG-спрайты Профика (idle, thinking, hop, cheer, run) | 10 |
| Анимации приёмов (тук, топ, дзынь, дрожь, вспышка) | 15 |
| Карта мира (свиток, оживление) | 12 |
| Раскрытие Осколка (3D-карточка, звук) | 8 |
| Профик с памятью (реплики + CloudStorage) | 5 |
| Интеграция с Ареной (XP, ачивки, стрик) | 4 |
| Полировка микро-деталей | 15 |
| Тестирование + баг-фиксы | 12 |
| **Итого срез** | **~144 часа** |

Полная игра (15 миров + финал) = **~500 часов**. Реалистично. Не 60 и не 600.

---

## 10. План тестирования на школьниках

### Кого зовём
- **20-30 школьников 8-11 класс** (микс: 5-6 сильных, 15-20 средних, 5-6 слабых по инфе).
- Тестируем **вживую** (можно созвонить в Zoom и попросить включить экран).

### Что смотрим
1. **Через 30 секунд** — понял ли, что делать? (Если не понял — фейл онбординга.)
2. **Через 5 минут** — улыбается ли, комментирует ли? (Если молчит и «делает по инерции» — фейл вовлечения.)
3. **Через 10 минут** — сам просит следующий уровень? Или ждёт когда закончится?
4. **На уровне 5-6** — понимает ли механику «Живого Python» или воспринимает как «просто выбор кнопок»?
5. **После финала мира** — восторг или вежливое «ок»?

### Что записываем
- Тайминг каждого уровня (тестер).
- Реплики школьника вслух.
- Точки, где путается или переспрашивает.
- Комментарий после: «поставь оценку 1-10» + «что понравилось / не понравилось».

### Критерии «победы»
- **≥ 70%** проходят все 10 уровней в первый заход.
- **≥ 60%** просят «а когда следующий мир?».
- **≥ 50%** оценка ≥ 8/10.
- **≥ 20%** после сессии говорят «покажу другу» без наводки.

Если эти цифры сходятся — идём в масштабирование. Если нет — знаем что чинить.

---

## 11. Что НЕ делаем в срезе (осознанно)

Всё это в бэклоге, но не сейчас:

- Другие миры (14 штук)
- Финал `>>> _` с `print("Спасибо.")`
- Prestige-режим
- Все альбомы кроме одной карточки
- Настоящий редактор кода
- Groups (класс с учителем)
- Экспорт кода в `.py`
- Свои задачи от учеников
- Ежедневный «Разогрев»
- «Пробник Профика»
- 2-3-4-5 этапы трансформации интерфейса (только 1-й — тап кнопок)

**Философия:** одна идеальная сцена > 15 средних. Одобрение — потом масштаб.

---

## 12. Финал полной игры (когда будет)

Из v3, с правкой эксперта. Оставляем на будущее, чтобы не забыть:

1. Все 15 миров пройдены, миры оживают на карте панорамой.
2. Профик садится рядом с игроком.
3. Открывается пустой `>>> _`.
4. Профик: «Теперь ты можешь писать свою историю.»
5. **Игрок печатает одну строку**: `print("Спасибо.")`
6. Консоль отвечает: `Спасибо.`
7. Профик улыбается. Кивает. Исчезает.
8. Титры.

Никаких сертификатов, никаких фанфар. Только тихое достоинство.

---

## 13. Что нужно от команды прямо сейчас

Перед стартом разработки — **3 решения**:

1. **Кто рисует спрайты Профика?** Нужны 5 состояний в едином стиле. Если нет иллюстратора — временный вариант через существующую SVG-Профик + анимация transform.

2. **Где берём звуки?** Freesound.org (CC0), zapsplat, или заказ у sound-дизайнера. MVP — CC0 хватит.

3. **Тестовая группа школьников** — есть ли доступ? Учитель может собрать 20-30 добровольцев через канал?

Если ответы «есть, есть, есть» — стартуем разработку.
Если хотя бы одно «нет» — сначала решаем.

---

## 14. Резюме

- **Не пишем больше концепта.** v3 достаточно.
- **Строим 1 мир (10 уровней), 144 часа работы, полная полировка.**
- **Даём 20-30 школьникам.**
- **Смотрим на 5 конкретных цифр.**
- **По результатам решаем: масштабировать или чинить.**

Готово к запуску разработки как только команда даст 3 «да» из раздела 13.
