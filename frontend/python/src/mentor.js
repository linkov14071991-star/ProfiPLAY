// mentor.js — «живой преподаватель»: голос Игоря, связь с каналом, рост личности.
// Профик остаётся маскотом, но постоянно ссылается на Игоря (по решению продукта).

export const CHANNEL_URL = 'https://t.me/profimatika_inf';
export const CHANNEL_NAME = '@profimatika_inf';

// Заметки Игоря о ЧАСТЫХ ОШИБКАХ — показываются после повторных промахов в теме.
// Это самое сильное и некопируемое: опыт преподавания.
export const IGOR_MISTAKES = {
  oge_print:  'Тут почти все путают кавычки. Запомни: print(2+3) считает, а print("2+3") печатает буквально. — Игорь',
  oge_vars:   'За годы вижу одно и то же: строку "7" и число 7 путают постоянно. Строки склеиваются, числа складываются. — Игорь',
  oge_input:  'Ошибка №1 у моих учеников: забыть int(). input() всегда даёт строку. Считываешь число — оборачивай в int(input()). — Игорь',
  oge_math:   'Остаток % и целое деление // — их путают чаще всего. n%10 — последняя цифра, n//10 убирает её. Заучи как таблицу умножения. — Игорь',
  oge_if:     'Классика: один = вместо двух ==. Один присваивает, два сравнивают. На экзамене эта опечатка стоит балла. — Игорь',
  oge_while:  'Самый частый провал: забыть менять счётчик в while — и цикл бесконечный. Всегда проверяй, что переменная условия меняется. — Игорь',
  oge_for:    'range(5) — это 0,1,2,3,4. Правая граница НЕ входит. На этом спотыкается половина класса каждый год. — Игорь',
  oge_str:    'Счёт символов с нуля! s[0] — первый. И обращение за границу (s[3] в слове из 3 букв) — ошибка. — Игорь',
  ege_lists:  'Индекс с нуля и здесь: a[1] — второй элемент. Плюс не путай len() (сколько) и sum() (сумма). — Игорь',
  ege_func:   'Главная путаница: return и print — разное! return отдаёт значение в программу, print только показывает. — Игорь',
  ege_str2:   'Срез s[a:b] — правая граница не входит, как и в range. И find() возвращает -1, если не нашёл, а не ошибку. — Игорь',
  ege_dict:   'Обращение d["нет_ключа"] — ошибка. Безопаснее d.get(ключ, 0). Это спасает на экзамене. — Игорь',
  ege_task24: 'В 24-й задаче губит жадность регулярок. Если .+ хватает лишнее — делай ленивым .+?. Проверял на трёх выпусках. — Игорь',
  ege_task25: 'В 25-й перебирай делители до корня (int(n**0.5)+1), иначе программа не успеет. И число проверяй через str(n). — Игорь',
  ege_task27: 'В 27-й задаче про звёзды всегда нужны две функции: dist и центр кластера. Заучи их как шаблон. — Игорь',
};

// Реплики РОСТА ЛИЧНОСТИ — после прохождения ключевой темы. Эмоция важнее цифр.
export const IDENTITY = {
  oge_print:  'Ты только что вывел первое на экран. Формально — ты уже написал программу. Дальше интереснее.',
  oge_vars:   'Теперь ты умеешь хранить данные. Это фундамент всего кода.',
  oge_input:  'Твоя программа теперь разговаривает с человеком. Уже не игрушка — инструмент.',
  oge_if:     'Ты научил программу принимать решения. Это уже настоящая логика.',
  oge_while:  'Циклы освоены — теперь ты умеешь заставлять компьютер повторять работу за тебя.',
  oge_for:    'Ты умеешь автоматизировать. Одна строка — и машина делает сотни действий.',
  oge_str:    'Работа с текстом — половина реальных задач. Ты её теперь умеешь.',
  ege_lists:  'Списки — сердце большинства задач ЕГЭ. Ты вышел на серьёзный уровень.',
  ege_func:   'Ты пишешь свои функции. Теперь твой код могут читать и переиспользовать другие.',
  ege_task25: 'Ты закрыл задачу №25 — одну из самых доходных на ЕГЭ. Это уже уровень.',
  ege_task24: 'Регулярки и задача №24 — не каждый выпускник их берёт. А ты взял.',
  ege_task27: 'Задача №27 — вершина второй части. Ты подобрался к максимальному баллу.',
};

// Предложение канала после ВАЖНЫХ тем (ненавязчиво). unitId → текст.
export const CHANNEL_HINTS = {
  oge_for:    'Игорь разбирал циклы на живых задачах в канале. Загляни — закрепишь.',
  ege_lists:  'В канале Игоря есть разбор типичной задачи на списки. Полезно посмотреть.',
  ege_task24: 'Игорь выкладывал разбор задачи №24 в канале. Рекомендую.',
  ege_task25: 'Похожую задачу №25 Игорь разбирал в канале пошагово.',
  ege_task27: 'Разбор задачи №27 у Игоря в канале — сложную тему проще понять с ним.',
};

// Адаптивный Профик — по ходу серии ответов (наставник).
export const ADAPTIVE = {
  struggling: 'Вижу, сегодня идёт тяжело. Это нормально — так учатся все. Давай спокойно, без спешки.',
  onFire:     'Ты сегодня в ударе! Если так дальше — скоро можно брать задачи ЕГЭ.',
};

// Пресеты экзамена: месяц/день, к которому идём. Отсчёт — до ближайшего наступления.
export const EXAM_PRESETS = [
  { key: 'ege', label: 'ЕГЭ по информатике', sub: 'начало июня', month: 6, day: 1 },
  { key: 'oge', label: 'ОГЭ по информатике', sub: 'конец мая', month: 5, day: 25 },
];

// Дней до ближайшего наступления даты месяц/день.
export function daysToExam(month, day) {
  if (!month || !day) return null;
  const now = new Date();
  const y = now.getFullYear();
  let target = new Date(y, month - 1, day);
  // если дата в этом году уже прошла — берём следующий год
  const today0 = new Date(y, now.getMonth(), now.getDate());
  if (target < today0) target = new Date(y + 1, month - 1, day);
  return Math.max(0, Math.round((target - today0) / 86400000));
}
