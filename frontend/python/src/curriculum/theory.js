// theory.js — краткая теория по каждому юниту.
// Показывается по желанию перед уроками. Ключ — unitId.
//
// Формат блока:
//   { h: 'Заголовок', t: 'Текст объяснения', code?: 'пример', out?: 'вывод примера' }
// videoIndex — позиция ролика в марафоне Py.Go (1-based), опционально.

export const THEORY = {
  // ── ОГЭ ──
  oge_print: {
    intro: 'Любая программа общается с человеком через вывод на экран. За это отвечает команда print — первое, что ты научишься делать. Всё, что стоит в скобках, окажется на экране.',
    blocks: [
      { h: 'Команда print', t: 'print печатает то, что стоит в скобках. Текст обязательно берётся в кавычки — одинарные или двойные, разницы нет.',
        code: 'print("Привет, мир!")', out: 'Привет, мир!',
        tip: 'Кавычки на экран не попадают — они лишь говорят Python «это текст».' },
      { h: 'Числа без кавычек', t: 'Если в скобках число или пример без кавычек — Python сначала посчитает, а потом выведет результат. С кавычками же он выведет всё буквально.',
        code: 'print(2 + 3)\nprint("2 + 3")', out: '5\n2 + 3',
        tip: 'Сравни: print(2+3) даёт 5, а print("2+3") даёт «2+3». Кавычки решают всё.' },
      { h: 'Несколько значений', t: 'Через запятую можно вывести несколько значений в одну строку — между ними Python сам поставит пробел.',
        code: 'print("Ответ:", 42)', out: 'Ответ: 42',
        tip: 'Запятая = пробел между значениями. Если пробел не нужен — склеивай строки плюсом.' },
      { h: 'Каждый print — с новой строки', t: 'По умолчанию каждый print переносит курсор на новую строку. Поэтому два print печатают на двух строках.',
        code: 'print("раз")\nprint("два")', out: 'раз\nдва',
        tip: 'Чтобы печатать в одну строку, добавь аргумент end=" ": print("раз", end=" ").' },
      { h: 'Комментарии', t: 'Всё, что идёт после символа #, Python не выполняет — это заметка для человека. Комментарии помогают не забыть, что делает код.',
        code: 'print("ок")  # это комментарий, его Python игнорирует' },
    ],
    videos: [
      { id: 'E8XjM4JvtrI', title: 'Установка и первая программа' },
    ],
  },

  oge_vars: {
    intro: 'Переменная — это коробка с именем, в которой хранится значение. Ты кладёшь туда число или текст, а потом обращаешься по имени. Значение в коробке можно менять сколько угодно раз.',
    blocks: [
      { h: 'Создание переменной', t: 'Знак = кладёт значение справа в переменную слева. Читается «положить в x пятёрку», а не «x равно 5».',
        code: 'x = 5\nprint(x)', out: '5',
        tip: 'Направление важно: слева переменная, справа значение. Наоборот (5 = x) — ошибка.' },
      { h: 'Значение можно менять', t: 'Каждое новое присваивание перезаписывает старое. В коробке всегда только последнее значение.',
        code: 'x = 5\nx = 10\nprint(x)', out: '10',
        tip: 'Частый приём: x = x + 1 — берём старое значение, увеличиваем, кладём обратно.' },
      { h: 'Числа и строки', t: 'В кавычках — строка (текст, тип str). Без кавычек цифры — число (тип int). Это разные типы, и вести себя они будут по-разному.',
        code: 'a = 7        # число int\nb = "7"      # строка str\nprint(a + a)\nprint(b + b)', out: '14\n77',
        tip: 'Число складывается (7+7=14), а строка склеивается ("7"+"7"="77"). Не перепутай!' },
      { h: 'Имена переменных', t: 'Имя состоит из букв, цифр и знака _, но не может начинаться с цифры и не может содержать пробелов. Регистр важен: x и X — разные переменные.',
        code: 'my_var = 1   # можно\nx2 = 2       # можно',
        tip: '2x, my var, my-var — так нельзя. Имя лучше делать понятным: summa, а не s1.' },
    ],
    videos: [
      { id: 'EwlgAUwV3BE', title: 'Вывод, типы данных и переменные' },
    ],
  },

  oge_input: {
    intro: 'Чтобы программа реагировала на человека, ей нужно считать введённые данные. За это отвечает input. Главное правило, которое ломает половину новичков: input всегда возвращает строку.',
    blocks: [
      { h: 'Команда input', t: 'input() ждёт, пока человек что-то напечатает и нажмёт Enter, и возвращает введённое как строку.',
        code: 'name = input()\nprint("Привет,", name)' },
      { h: 'Важно: input даёт строку', t: 'Даже если ты ввёл цифры, для Python это текст. Поэтому "5" + "5" не 10, а склейка "55".',
        code: 'x = input()   # ввели 5\nprint(x + x)', out: '55',
        tip: 'Это самая частая ошибка на входе. Если нужно считать, оборачивай в int().' },
      { h: 'Превращаем в число', t: 'int() превращает строку в целое число. Только после этого с введённым можно считать.',
        code: 'x = int(input())  # ввели 5\nprint(x + x)', out: '10',
        tip: 'Запомни готовую конструкцию: x = int(input()) — считать целое число.' },
      { h: 'Дробные числа', t: 'Если число может быть с точкой (например 3.5), используй float(input()) вместо int.',
        code: 'x = float(input())  # ввели 2.5\nprint(x * 2)', out: '5.0' },
    ],
    videos: [],  // в марафоне Py.Go нет отдельного ролика про input
  },

  oge_math: {
    intro: 'Python умеет считать. Кроме обычных + - * есть важные для экзамена деление, остаток и степень.',
    blocks: [
      { h: 'Деление нацело //', t: 'Отбрасывает дробную часть. 17 // 5 = 3.',
        code: 'print(17 // 5)', out: '3' },
      { h: 'Остаток от деления %', t: 'Сколько осталось после деления нацело. 17 % 5 = 2, потому что 17 = 5·3 + 2. Остаток — важнейший инструмент на ЕГЭ.',
        code: 'print(17 % 5)', out: '2',
        tip: 'n % 10 даёт последнюю цифру числа, n // 10 убирает её — так разбирают число на цифры.' },
      { h: 'Чётность через остаток', t: 'Число чётное, если делится на 2 без остатка, то есть x % 2 == 0. Результат сравнения — True или False.',
        code: 'x = 10\nprint(x % 2 == 0)', out: 'True',
        tip: 'Так же проверяют делимость на любое число: кратно 3 — это x % 3 == 0.' },
      { h: 'Степень **', t: 'Возведение в степень. 2 ** 10 = 1024.',
        code: 'print(2 ** 10)', out: '1024' },
      { h: 'Обычное деление /', t: 'Один слэш всегда даёт дробь: 7 / 2 = 3.5.',
        code: 'print(7 / 2)', out: '3.5' },
    ],
    videos: [
      { id: 'Vn6nS4xBJlg', title: 'Арифметика чисел' },
      { id: 'sEFm3hKv5Gc', title: 'Вещественные числа и тип float' },
      { id: 'LTX9YjRabPE', title: 'Арифметика строк' },
    ],
  },

  oge_if: {
    intro: 'Программа может выбирать путь в зависимости от условия. Это делает if.',
    blocks: [
      { h: 'if — если', t: 'Если условие истинно — выполнится код с отступом.',
        code: 'x = 7\nif x > 5:\n    print("много")', out: 'много' },
      { h: 'else — иначе', t: 'Что делать, если условие ложно.',
        code: 'x = 3\nif x > 5:\n    print("много")\nelse:\n    print("мало")', out: 'мало' },
      { h: 'Сравнения', t: 'Равно ==, не равно !=, больше >, меньше <, >=, <=. Внимание: == сравнивает, а один = присваивает.' },
      { h: 'elif — иначе если', t: 'Проверить ещё условие, если предыдущее не сработало.',
        code: 'if x > 0:\n    print("плюс")\nelif x < 0:\n    print("минус")\nelse:\n    print("ноль")' },
      { h: 'Отступы важны', t: 'Тело if пишется с отступом (4 пробела). Без отступа Python не поймёт, что относится к условию.' },
    ],
    videos: [
      { id: 'jsRZrw8HIGQ', title: 'Условный оператор, операции сравнения' },
      { id: '04hmvtSbfSg', title: 'Составные условия, логический тип' },
    ],
  },

  oge_while: {
    intro: 'Цикл while повторяет действия, пока условие остаётся истинным.',
    blocks: [
      { h: 'Пока верно — повторяй', t: 'Тело while выполняется снова и снова, пока условие True.',
        code: 'i = 1\nwhile i <= 3:\n    print(i)\n    i = i + 1', out: '1\n2\n3' },
      { h: 'Счётчик обязателен', t: 'Внутри цикла обязательно нужно менять переменную из условия (например, i = i + 1). Если этого не сделать, условие всегда останется истинным и цикл никогда не закончится.',
        code: 'i = 1\nwhile i <= 3:\n    print(i)\n    i = i + 1', out: '1\n2\n3',
        tip: 'Забыл i = i + 1 — получил вечный цикл. Это ошибка №1 в while.' },
      { h: 'Накопление суммы', t: 'Очень частый приём: заводим накопитель s = 0 до цикла и в каждом шаге что-то прибавляем. После цикла в s лежит итог.',
        code: 's = 0\ni = 1\nwhile i <= 4:\n    s = s + i\n    i = i + 1\nprint(s)', out: '10',
        tip: 'Тот же приём работает и с for. Накопитель заводят ДО цикла, печатают ПОСЛЕ.' },
      { h: 'Разбор числа на цифры', t: 'Классика ЕГЭ: n % 10 берёт последнюю цифру, n // 10 отрезает её. Повторяем, пока число не станет 0.',
        code: 'n = 253\ns = 0\nwhile n > 0:\n    s = s + n % 10\n    n = n // 10\nprint(s)', out: '10',
        tip: 'Этот шаблон (сумма/произведение/количество цифр) встречается на экзамене постоянно.' },
    ],
    videos: [
      { id: 'XGgCn95Qi90', title: 'Цикл WHILE' },
    ],
  },

  oge_for: {
    intro: 'Когда известно, сколько раз повторить, удобнее for с range.',
    blocks: [
      { h: 'for и range', t: 'range(n) даёт числа от 0 до n-1. Счёт начинается с нуля, а правая граница НЕ входит. Цикл for проходит по этим числам по очереди.',
        code: 'for i in range(3):\n    print(i)', out: '0\n1\n2',
        tip: 'range(3) — это три числа: 0, 1, 2. Само число 3 не входит. Это ловушка №1 на ЕГЭ.' },
      { h: 'range с началом', t: 'range(1, 4) — от 1 до 3 включительно. Первое число входит, последнее (4) — нет.',
        code: 'for i in range(1, 4):\n    print(i)', out: '1\n2\n3',
        tip: 'Сколько чисел в range(a, b)? Ровно b - a штук.' },
      { h: 'range с шагом', t: 'Третий аргумент — шаг, размер прыжка. range(0, 10, 2) идёт через одно: 0, 2, 4, 6, 8.',
        code: 'for i in range(0, 10, 2):\n    print(i)', out: '0\n2\n4\n6\n8' },
      { h: 'Шаг назад', t: 'Отрицательный шаг заставляет цикл идти вниз. range(5, 0, -1) даёт 5, 4, 3, 2, 1.',
        code: 'for i in range(5, 0, -1):\n    print(i)', out: '5\n4\n3\n2\n1',
        tip: 'Чтобы идти вниз, нужен именно шаг -1. Без него range(5, 0) будет пустым.' },
      { h: 'Подсчёт по условию', t: 'Классика ОГЭ — считать, сколько чисел подходят под условие.',
        code: 'k = 0\nfor i in range(1, 31):\n    if i % 7 == 0:\n        k = k + 1\nprint(k)', out: '4' },
    ],
    videos: [
      { id: '2wt6UGUFRGQ', title: 'Переменная цикла FOR' },
      { id: 'iMCjAytQWmk', title: 'Варианты цикла FOR' },
    ],
  },

  oge_str: {
    intro: 'Строка — это последовательность символов. С ней можно многое: узнать длину, взять символ, перебрать.',
    blocks: [
      { h: 'Длина строки', t: 'len() возвращает количество символов.',
        code: 's = "Привет"\nprint(len(s))', out: '6' },
      { h: 'Обращение к символу', t: 'К каждому символу можно обратиться по номеру (индексу) в квадратных скобках. Нумерация начинается с нуля: s[0] — первый символ, s[1] — второй.',
        code: 's = "кот"\nprint(s[0])', out: 'к',
        tip: 'Первый символ — это s[0], а не s[1]. Счёт с нуля — постоянный источник ошибок.' },
      { h: 'Отрицательные индексы', t: 'Отрицательные индексы считают с конца: s[-1] — последний символ, s[-2] — предпоследний. Удобно, когда не знаешь длину.',
        code: 's = "кот"\nprint(s[-1])', out: 'т',
        tip: 'Обращение за границу (например s[3] в строке из 3 букв) — ошибка IndexError.' },
      { h: 'Перебор строки', t: 'for проходит строку по буквам.',
        code: 'for c in "abc":\n    print(c)', out: 'a\nb\nc' },
      { h: 'Повтор строки', t: 'Строка * число повторяет её.',
        code: 'print("ба" * 3)', out: 'бабаба' },
    ],
    videos: [
      { id: 'DLHAesA64Ow', title: 'Индексы строк' },
      { id: 'wCPKfkLrYRg', title: 'Срезы строк' },
      { id: 'A4pqJe8byTg', title: 'Сравнение строк' },
      { id: '-tS7GAZbx44', title: 'Методы строк' },
    ],
  },

  // ── ЕГЭ ──
  ege_lists: {
    intro: 'Список хранит много значений под одним именем. Основа большинства задач ЕГЭ.',
    blocks: [
      { h: 'Что такое список', t: 'Список хранит много значений под одним именем. Значения пишут в квадратных скобках через запятую. Доступ к элементу — по индексу, счёт с нуля.',
        code: 'a = [10, 20, 30]\nprint(a[1])', out: '20',
        tip: 'a[1] — это второй элемент (20), а не первый. Первый — a[0].' },
      { h: 'Длина и сумма', t: 'len(a) — количество элементов, sum(a) — сумма всех чисел. Эти встроенные функции экономят целый цикл.',
        code: 'a = [10, 20, 30]\nprint(sum(a))', out: '60',
        tip: 'Среднее арифметическое = sum(a) / len(a). Часто спрашивают на ЕГЭ.' },
      { h: 'Добавление', t: 'append() добавляет элемент в конец.',
        code: 'a = [1, 2, 3]\na.append(4)\nprint(a)', out: '[1, 2, 3, 4]' },
      { h: 'Сортировка', t: 'sort() упорядочивает по возрастанию.',
        code: 'a = [3, 1, 2]\na.sort()\nprint(a)', out: '[1, 2, 3]' },
      { h: 'Максимум и минимум', t: 'max() и min() находят крайние значения.',
        code: 'a = [5, 2, 8, 1]\nprint(max(a), min(a))', out: '8 1' },
      { h: 'Срезы', t: 'a[1:3] — часть списка с индекса 1 по 2 (3 не входит).',
        code: 'a = [0, 1, 2, 3, 4]\nprint(a[1:3])', out: '[1, 2]' },
    ],
    videos: [
      { id: 'dci0W14OfPk', title: 'Массивы и основные операции' },
      { id: 'tcm9jWdp-6o', title: 'Добавление элементов в массив' },
      { id: '8FYwxmYq2jk', title: 'Индексы элементов, изменение, срезы' },
      { id: '9MX_eeIy1mI', title: 'Два типа циклов по массиву' },
      { id: '97dOQ9pDSyY', title: 'Методы index, count, pop, remove, insert' },
      { id: 'pEqNSdOKRm4', title: 'Встроенные: in, not in, max, min' },
      { id: 'GISy6STCYdQ', title: 'Изменяемые типы. Изменение массивов' },
    ],
  },

  ege_nested: {
    intro: 'Цикл можно поместить внутрь другого цикла. Это нужно для таблиц, пар, перебора всех сочетаний.',
    blocks: [
      { h: 'Цикл в цикле', t: 'Для каждого шага внешнего цикла внутренний проходит целиком.',
        code: 'for i in range(1, 3):\n    for j in range(1, 3):\n        print(i, j)', out: '1 1\n1 2\n2 1\n2 2' },
      { h: 'Число повторений перемножается', t: 'Внешний 3 раза × внутренний 2 = 6 итераций тела.',
        code: 'k = 0\nfor i in range(3):\n    for j in range(2):\n        k = k + 1\nprint(k)', out: '6' },
    ],
    videos: [
      { id: '9MX_eeIy1mI', title: 'Два типа циклов по массиву' },
    ],
  },

  ege_func: {
    intro: 'Функция — это своя команда. Записал один раз — используешь много.',
    blocks: [
      { h: 'Определение функции', t: 'def задаёт функцию — свою команду. В скобках параметры (входные данные), а return отдаёт результат обратно в программу. Функцию сначала определяют, потом вызывают по имени.',
        code: 'def kvadrat(x):\n    return x * x\nprint(kvadrat(5))', out: '25',
        tip: 'return и print — разные вещи! return отдаёт значение для дальнейших вычислений, print только показывает на экране.' },
      { h: 'Несколько параметров', t: 'Функция может принимать несколько значений.',
        code: 'def summa(a, b):\n    return a + b\nprint(summa(3, 4))', out: '7' },
      { h: 'Функция может вернуть True/False', t: 'Удобно для проверок.',
        code: 'def chet(n):\n    return n % 2 == 0\nprint(chet(10))', out: 'True' },
      { h: 'return завершает функцию', t: 'После return функция сразу отдаёт значение и заканчивается.' },
    ],
    videos: [
      { id: 'BSJHrx1JmJg', title: 'Функции, основные понятия. Параметры' },
      { id: 'sH9_nKZCLdY', title: 'Значение функции. Оператор return' },
    ],
  },

  ege_str2: {
    intro: 'Кроме индексов у строк есть срезы и методы — мощные инструменты для задач на строки.',
    blocks: [
      { h: 'Срезы строк', t: 's[0:4] — символы с 0 по 3.',
        code: 's = "информатика"\nprint(s[0:4])', out: 'инфо' },
      { h: 'Переворот строки', t: 's[::-1] переворачивает строку задом наперёд.',
        code: 'print("Python"[::-1])', out: 'nohtyP' },
      { h: 'Регистр', t: 'upper() — заглавные, lower() — строчные.',
        code: 'print("abc".upper())', out: 'ABC' },
      { h: 'Подсчёт и поиск', t: 'count() считает вхождения подстроки.',
        code: 'print("hello".count("l"))', out: '2' },
      { h: 'Разбиение', t: 'split() разбивает строку в список по разделителю.',
        code: 'print("1,2,3".split(","))', out: "['1', '2', '3']" },
    ],
    videos: [
      { id: 'wCPKfkLrYRg', title: 'Срезы строк' },
      { id: '-tS7GAZbx44', title: 'Методы строк' },
      { id: 'OrYbn1b5CW0', title: 'Методы join() и split()' },
    ],
  },

  ege_dict: {
    intro: 'Словарь хранит пары «ключ → значение». Множество хранит только уникальные элементы.',
    blocks: [
      { h: 'Словарь', t: 'Доступ к значению по ключу, а не по индексу.',
        code: 'd = {"a": 1, "b": 2}\nprint(d["b"])', out: '2' },
      { h: 'Подсчёт частот', t: 'get(ключ, 0) даёт текущее значение или 0. Классика для подсчёта символов.',
        code: 'd = {}\nfor c in "aab":\n    d[c] = d.get(c, 0) + 1\nprint(d["a"])', out: '2' },
      { h: 'Множество', t: 'set хранит только уникальные значения. Дубликаты исчезают.',
        code: 's = {1, 2, 2, 3, 3, 3}\nprint(len(s))', out: '3' },
    ],
    videos: [],  // в марафоне Py.Go нет отдельного ролика про словари
  },

  ege_lib: {
    intro: 'Библиотеки добавляют готовые функции. Для ЕГЭ важны math и random.',
    blocks: [
      { h: 'Подключение', t: 'import math делает доступными функции модуля.',
        code: 'import math\nprint(math.sqrt(16))', out: '4.0' },
      { h: 'Факториал', t: 'math.factorial(n) = 1·2·...·n.',
        code: 'import math\nprint(math.factorial(4))', out: '24' },
      { h: 'Число пи', t: 'math.pi — константа 3.1415…',
        code: 'import math\nprint(math.pi > 3)', out: 'True' },
      { h: 'Случайные числа', t: 'random.randint(a, b) — случайное целое от a до b включительно.',
        code: 'import random\nx = random.randint(1, 6)  # 1..6' },
    ],
    videos: [
      { id: 'jLWEAQIOcmQ', title: 'Стандартная библиотека Python. Модули' },
    ],
  },

  ege_math_full: {
    intro: 'math — самая частая библиотека на ЕГЭ, нужна почти везде, где есть вычисления (задачи 7, 11, 14, 16, 25, 27). Подключать удобнее по частям: from math import ceil, sqrt, gcd — тогда пишешь без приставки math.',
    blocks: [
      { h: 'Три способа подключить', t: 'from math import ceil — берём один инструмент; from math import * — берём всё сразу; import math — целиком, но тогда обращаемся как math.ceil(...). На ЕГЭ удобнее первый или второй способ.' },
      { h: 'Округление', t: 'ceil(x) округляет ВВЕРХ, floor(x) — ВНИЗ, round(x) — к ближайшему целому. Важно: round использует банковское округление (round(2.5) = 2, а не 3).',
        code: 'from math import ceil, floor\nprint(ceil(3.2), floor(3.8))', out: '4 3',
        tip: 'Для «сколько нужно контейнеров/страниц» всегда ceil — округление вверх.' },
      { h: 'Корень и степень', t: 'sqrt(x) — квадратный корень (результат дробный). То же самое можно получить как x ** 0.5. pow(a, b) — возведение в степень, то же, что a ** b.',
        code: 'from math import sqrt\nprint(sqrt(16), 16 ** 0.5)', out: '4.0 4.0' },
      { h: 'НОД (наибольший общий делитель)', t: 'gcd(a, b) — наибольшее число, на которое делятся оба. В Python 3.9+ можно передавать несколько чисел сразу.',
        code: 'from math import gcd\nprint(gcd(12, 18), gcd(24, 36, 60))', out: '6 12' },
      { h: 'Логарифмы (главное в задаче 11)', t: 'log2(x) — логарифм по основанию 2. log(x, b) — по любому основанию b. Количество бит на один символ из алфавита в N символов = ceil(log2(N)).',
        code: 'from math import ceil, log2\nprint(ceil(log2(1000)))', out: '10',
        tip: 'log2 иногда даёт неточный результат из-за дробей. Для числа бит всегда оборачивай в ceil.' },
      { h: 'Факториал и константы', t: 'factorial(n) = n! = 1·2·...·n (комбинаторика). Константы: pi ≈ 3.14159, e ≈ 2.71828.',
        code: 'from math import factorial, pi\nprint(factorial(5))\nprint(round(pi, 2))', out: '120\n3.14' },
      { h: 'Полная шпаргалка math', t: 'ceil/floor — округление; sqrt — корень; gcd — НОД; log2(x) и log(x,b) — логарифмы; factorial(n) — факториал; pow(a,b) — степень; dist(a,b) — расстояние; pi, e — константы.' },
    ],
    videos: [{ id: 'jLWEAQIOcmQ', title: 'Стандартная библиотека Python. Модули' }],
  },

  ege_itertools: {
    intro: 'itertools перебирает за тебя все комбинации — не нужно писать вложенные циклы вручную. Главный инструмент задачи 8 (product) и подспорье в задаче 15 (combinations). Все функции возвращают кортежи.',
    blocks: [
      { h: 'product — слова с повторами', t: 'product(символы, repeat=n) даёт ВСЕ слова длины n, где символы могут повторяться. Это основа задачи 8: перебираем все коды и считаем подходящие.',
        code: "from itertools import product\nfor p in product('АБ', repeat=2):\n    print(''.join(p))", out: 'АА\nАБ\nБА\nББ',
        tip: 'Сколько всего слов? Число символов в степени длины: тут 2² = 4.' },
      { h: 'permutations — перестановки', t: 'permutations(символы, n) — все способы выбрать n элементов, где порядок ВАЖЕН и повторов НЕТ. Из «АБВ» по 2: АБ и БА считаются разными.',
        code: "from itertools import permutations\nprint(len(list(permutations('АБВ', 2))))", out: '6',
        tip: 'Количество: n! / (n-k)!. Из 3 по 2 = 3·2 = 6.' },
      { h: 'combinations — сочетания', t: 'combinations(символы, n) — выбор n элементов, где порядок НЕважен и повторов нет. (1,2) и (2,1) — одно и то же, берётся один раз. Помогает в задаче 15 на отрезки.',
        code: 'from itertools import combinations\nfor c in combinations([1, 2, 3], 2):\n    print(c)', out: '(1, 2)\n(1, 3)\n(2, 3)' },
      { h: 'combinations_with_replacement', t: 'То же, что combinations, но элемент можно брать повторно: (1,1), (1,2)…',
        code: 'from itertools import combinations_with_replacement as cwr\nprint(len(list(cwr([1, 2, 3], 2))))', out: '6' },
      { h: 'Главный подвох', t: 'Все эти функции дают КОРТЕЖИ, а не строки. Чтобы получить слово, склей кортеж: "".join(p). Порядок аргументов в combinations важен: первый — откуда выбираем, второй — сколько.' },
      { h: 'Шпаргалка', t: 'product(s, repeat=n) — слова с повторами (задача 8); permutations(s, n) — перестановки без повторов; combinations(s, n) — сочетания без учёта порядка (задача 15); combinations_with_replacement — сочетания с повторами.' },
    ],
    videos: [],
  },

  ege_recursion: {
    intro: 'Задача 16 — про рекурсивные функции. Когда рекурсия глубокая (большие n), она тормозит или падает. Спасают два приёма: кэш @lru_cache или замена рекурсии на заполнение массива.',
    blocks: [
      { h: 'Что такое рекурсия', t: 'Функция вызывает саму себя. Обязателен базовый случай — условие, при котором функция перестаёт вызывать себя и возвращает готовый ответ. Без него — бесконечный вызов.',
        code: 'def F(n):\n    if n <= 2:\n        return n\n    return F(n - 1) + F(n - 2)\nprint(F(6))', out: '13' },
      { h: 'Кэш @lru_cache', t: '@lru_cache запоминает уже посчитанные значения, чтобы не считать их заново. Ставится строго НАД def, без пустой строки. Число в скобках — размер кэша (обычно 10000 или больше).',
        code: 'from functools import lru_cache\n@lru_cache(10000)\ndef G(n):\n    if n <= 20:\n        return n + 2\n    return G(n - 3) + 1',
        tip: 'Без кэша функция вроде чисел Фибоначчи считает одно и то же тысячи раз и зависает.' },
      { h: 'Надёжнее — массив без рекурсии', t: 'Самый безопасный способ для больших n: завести массив F и заполнить его циклом от начала к концу. Никакой глубокой рекурсии — ничего не упадёт.',
        code: 'F = [0] * 100\nfor n in range(100):\n    if n < 10:\n        F[n] = n + 2\n    else:\n        F[n] = F[n - 3] + 1\nprint(F[50])', out: '24' },
      { h: 'НЕ поднимай лимит рекурсии', t: 'Команда setrecursionlimit теоретически разрешает больше вызовов, но на экзамене работает нестабильно — Python может упасть прямо на проверке, и баллы потеряны. Всегда выбирай @lru_cache или массив.' },
      { h: 'Шпаргалка', t: '@lru_cache(N) над функцией — кэш до N значений; @lru_cache(maxsize=None) — без ограничения. Массив F[n], заполненный циклом — самый надёжный вариант для задачи 16.' },
    ],
    videos: [],
  },

  ege_re: {
    intro: 'Регулярные выражения (модуль re) — мощный способ искать шаблоны в длинной строке. Главный инструмент задачи 24. Ты описываешь «маску» из спецсимволов, а Python находит все куски строки, которые под неё подходят.',
    blocks: [
      { h: 'finditer — главная функция', t: 'finditer проходит по ВСЕМ совпадениям в строке. У каждого совпадения m есть m.group() — сам текст, m.start() и m.end() — индексы начала и конца. Именно finditer чаще всего нужен в задаче 24.',
        code: "from re import finditer\ns = 'A12B345C6'\nfor m in finditer(r'[0-9]+', s):\n    print(m.group())", out: '12\n345\n6',
        tip: 'm.start() и m.end() дают позицию совпадения — часто нужны, чтобы проверить, что идёт рядом.' },
      { h: 'findall — просто список', t: 'Если позиции не нужны, findall сразу возвращает список всех совпадений — короче, чем finditer.',
        code: "from re import findall\nprint(findall(r'[0-9]+', 'A12B345C6'))", out: "['12', '345', '6']" },
      { h: 'search и fullmatch', t: 'search — первое совпадение (или None). fullmatch — подходит ли ВСЯ строка целиком под шаблон.',
        code: "from re import search\nprint(search(r'[0-9]+', 'ab12cd').group())", out: '12' },
      { h: 'Символьные классы', t: 'Задают, какой символ разрешён в одной позиции: [ABC] — один из A,B,C; [A-Z] — любая буква диапазона; [0-9] — цифра; [^A] — любой символ, КРОМЕ A; точка . — вообще любой символ.' },
      { h: 'Квантификаторы (сколько раз)', t: 'Ставятся после символа/класса: * — 0 и больше повторов; + — 1 и больше; ? — 0 или 1; {n} — ровно n раз; {n,m} — от n до m раз.',
        code: "from re import findall\nprint(findall(r'[0-9]{2,3}', 'a1b22c333'))", out: "['22', '333']" },
      { h: 'Жадность и лень (ключ задачи 24!)', t: 'По умолчанию квантификаторы жадные — захватывают КАК МОЖНО БОЛЬШЕ. Добавишь ? после квантификатора — станут ленивыми и возьмут МИНИМУМ. Из-за жадности можно случайно склеить несколько чисел в одно.',
        code: "from re import findall\nprint(findall(r'<.+>', '<a><b>'))\nprint(findall(r'<.+?>', '<a><b>'))", out: "['<a><b>']\n['<a>', '<b>']",
        tip: 'Жадные: * + ? {n,m}. Ленивые: *? +? ?? {n,m}?. При поиске чисел между разделителями почти всегда нужна лень.' },
      { h: 'Группировка и альтернатива', t: '(AB)+ — пара AB повторяется; AB|CD — либо AB, либо CD. Чтобы искать сам спецсимвол, его экранируют: \\* \\+ \\. \\( \\).' },
      { h: 'Префикс r обязателен', t: 'Всегда пиши r"шаблон" (raw-строка). Тогда обратный слэш \\ Python не трогает, и он достаётся регулярке как есть.' },
    ],
    videos: [],
  },

  ege_fnmatch: {
    intro: 'fnmatch удобно проверяет, подходит ли число под «шаблон с дырками». Главный инструмент задачи 25. Работает только со строками, поэтому число всегда оборачивают в str(n).',
    blocks: [
      { h: 'Знак ? — один символ', t: '? в маске означает ровно ОДИН любой символ на этом месте.',
        code: 'from fnmatch import fnmatch\nprint(fnmatch("54213", "54?1?"))', out: 'True',
        tip: '«54?1?» подходит к 54213: 5-4-(любой)-1-(любой).' },
      { h: 'Знак * — любое количество', t: '* заменяет любое число символов, в том числе ноль.',
        code: 'from fnmatch import fnmatch\nprint(fnmatch("12700", "1*0"))', out: 'True' },
      { h: 'Класс [123]', t: '[123] на месте — один из перечисленных символов. Например, «1[57]2» подходит к 152 и 172.',
        code: 'from fnmatch import fnmatch\nprint(fnmatch("152", "1[57]2"))', out: 'True' },
      { h: 'Только строки!', t: 'fnmatch принимает строки. Число обязательно превращают: fnmatch(str(n), "1*7"). Если передать число — будет ошибка.' },
      { h: 'Шаблон задачи 25', t: 'Перебираем нужные числа (например, из диапазона с шагом) и проверяем маску str(n). Часто нужно найти первые 5 подходящих.',
        code: 'from fnmatch import fnmatch\nk = 0\nfor n in range(100, 1000, 7):\n    if fnmatch(str(n), "1*2"):\n        print(n)\n        k += 1\n        if k == 3:\n            break' },
      { h: 'Шпаргалка', t: '? — ровно один любой символ; * — любое количество (в т.ч. 0); [123] — один из перечисленных. Проверяй число через str(n).' },
    ],
    videos: [],
  },

  ege_task8: {
    intro: 'Задача 8 — про подсчёт слов и кодов по правилам. Инструмент один — product из itertools. Главное: правильно записать условие внутри цикла.',
    blocks: [
      { h: 'Идея', t: 'product перебирает ВСЕ слова заданной длины с повторами. Мы проходим их и считаем те, что подходят под условие задачи.',
        code: "from itertools import product\nk = 0\nfor w in product('АБВ', repeat=3):\n    # тут условие задачи\n    k += 1\nprint(k)", out: '27' },
      { h: 'Шаблон с условием', t: 'Обычно нужно проверить соседние буквы или наличие/отсутствие символа.',
        code: "from itertools import product\nk = 0\nfor w in product('01', repeat=4):\n    if '11' not in ''.join(w):\n        k += 1\nprint(k)", out: '8' },
      { h: 'Частая ошибка', t: 'Сравнивая соседей w[i] и w[i+1], бери range(len-1), иначе выход за границу.',
        tip: 'Склеивай кортеж в строку через "".join(w) — так удобнее искать подстроки.' },
    ],
    videos: [],
  },

  ege_task24: {
    intro: 'Задача 24 — обработка длинной строки из файла. Алгоритм: прочитать строку целиком → пройти finditer по всем совпадениям шаблона → проверить условие. Ключевые темы: finditer и жадность/лень.',
    blocks: [
      { h: 'Чтение строки', t: 'Всю строку берут одной командой: s = open("f.txt").readline(). Дальше ищут в s шаблоны.',
        code: "from re import findall\ns = 'A12BB345'\nprint(findall(r'[0-9]+', s))", out: "['12', '345']" },
      { h: 'finditer с позициями', t: 'finditer даёт каждое совпадение как объект m: m.group() — текст, m.start()/m.end() — где оно стоит. Позиции нужны, чтобы посмотреть, что идёт до или после найденного числа.',
        code: "from re import finditer\nfor m in finditer(r'[0-9]+', 'A12BB345'):\n    print(m.group(), m.start(), m.end())", out: '12 1 3\n345 5 8' },
      { h: 'Жадность и лень', t: 'По умолчанию + и * жадные (берут максимум). Добавь ? — станут ленивыми (минимум). В задаче 24 лень спасает от склейки соседних совпадений в одно.',
        code: "from re import findall\nprint(findall(r'<.+>', '<a><b>'))\nprint(findall(r'<.+?>', '<a><b>'))", out: "['<a><b>']\n['<a>', '<b>']" },
      { h: 'Шаблон числа', t: 'Например, 14-ричное без ведущего нуля, кончается чётной цифрой: [1-9A-D][0-9A-D]*[02468AC]. Первый класс без нуля, средний — любые цифры, последний — только чётные.',
        tip: 'Первая цифра не ноль → диапазон начинается с 1. Всегда пиши r"" перед шаблоном.' },
    ],
    videos: [],
  },

  ege_task25: {
    intro: 'Задача 25 — про делители и маски чисел. Два инструмента: fnmatch (маска) и перебор делителей до корня. Шаблон нужно знать наизусть.',
    blocks: [
      { h: 'Маска через fnmatch', t: '? — один символ, * — любое количество. Число проверяют как строку.',
        code: 'from fnmatch import fnmatch\nprint(fnmatch("15243", "1?2*"))', out: 'True' },
      { h: 'Поиск делителя (до корня!)', t: 'Делители идут парами: если d делит n, то и n//d. Достаточно дойти до √n — в разы быстрее.',
        code: 'def div(n):\n    for d in range(2, int(n ** 0.5) + 1):\n        if n % d == 0:\n            if d % 10 == 7:\n                return d\n            if (n // d) % 10 == 7:\n                return n // d\n    return 0\nprint(div(119))', out: '7' },
      { h: 'Шаблон перебора', t: 'Идём по нужным числам, проверяем маску или делитель, набираем первые 5.',
        code: 'from fnmatch import fnmatch\nk = 0\nfor n in range(100, 1000, 7):\n    if fnmatch(str(n), "1*7"):\n        print(n)\n        k += 1\n        if k == 5:\n            break',
        tip: 'Число всегда оборачивай в str(n) — fnmatch работает только со строками.' },
    ],
    videos: [],
  },

  ege_task27: {
    intro: 'Задача 27 — обработка массива данных (часто точки/звёзды на плоскости). Обязательный шаблон: функция dist и поиск центра кластера.',
    blocks: [
      { h: 'Расстояние', t: 'Между точками — теорема Пифагора. Есть готовая math.dist или пишут свою.',
        code: 'from math import dist\nprint(dist([0, 0], [5, 12]))', out: '13.0' },
      { h: 'Центр кластера (медоид)', t: 'Точка с минимальной суммой расстояний до всех остальных. Ищут через min с ключом.',
        code: 'from math import dist\npts = [[0, 0], [3, 4], [6, 8]]\nbest = min(pts, key=lambda p: sum(dist(p, q) for q in pts))\nprint(best)', out: '[3, 4]' },
      { h: 'Округление ответа', t: 'Ответ часто нужен с 2 знаками: f"{d:.2f}".',
        tip: 'Чтение точек: для каждой строки [int(x) for x in line.split()].' },
    ],
    videos: [],
  },

  ege_files: {
    intro: 'f-строки форматируют вывод (двоичное, hex, округление), а чтение из файла нужно в задачах 9, 17, 24.',
    blocks: [
      { h: 'Подстановка значений', t: 'f"...{x}..." подставляет переменную.',
        code: 'x = 42\nprint(f"ответ {x}")', out: 'ответ 42' },
      { h: 'Двоичное с нулями', t: '{x:08b} — двоичная запись длиной 8. Для IP — {x:032b}.',
        code: 'print(f"{5:08b}")', out: '00000101' },
      { h: 'Системы счисления', t: '{x:x} — 16-ричное, {x:o} — 8-ричное.',
        code: 'print(f"{255:x}")', out: 'ff' },
      { h: 'Округление', t: '{x:.2f} — 2 знака после запятой (задача 27).',
        code: 'print(f"{3.14159:.2f}")', out: '3.14' },
      { h: 'Чтение файла', t: 'Три шаблона: по числу в строке [int(x) for x in open("f.txt")]; через пробел line.split(); одна строка open("f.txt").readline().' },
    ],
    videos: [],
  },

  ege_ipaddress: {
    intro: 'ipaddress — готовый инструмент для задачи 13. Сам считает адрес сети, broadcast, маску, диапазон и количество адресов. Не нужно возиться с двоичными масками вручную.',
    blocks: [
      { h: 'Создание сети', t: 'ip_network("адрес/маска", strict=False). Маску можно писать полной (255.255.255.0) или числом единиц (/24). strict=False (или просто 0) обязателен, иначе на адресе узла будет ошибка.',
        code: "from ipaddress import ip_network\nnet = ip_network('192.168.1.10/255.255.255.0', strict=False)\nprint(net[0])", out: '192.168.1.0' },
      { h: 'Ключевые адреса', t: 'net[0] — адрес сети (первый), net[-1] — broadcast (последний), net[1] — минимальный адрес для компьютера, net[-2] — максимальный адрес для компьютера.',
        code: "from ipaddress import ip_network\nnet = ip_network('192.168.1.0/24', strict=False)\nprint(net[1], net[-2])", out: '192.168.1.1 192.168.1.254' },
      { h: 'Маска и количество', t: 'net.netmask — маска сети, net.num_addresses — всего адресов в сети, а число узлов (компьютеров) = num_addresses - 2 (минус сеть и broadcast).',
        code: "from ipaddress import ip_network\nnet = ip_network('10.0.0.0/24', strict=False)\nprint(net.num_addresses)", out: '256' },
      { h: 'Перебор IP с условием', t: 'По сети можно пройтись циклом. Для условия часто переводят адрес в 32-битную двоичную запись: f"{int(ip):032b}", а потом считают единицы через .count("1").' },
      { h: 'Шпаргалка', t: 'ip_network("адрес/маска", strict=False); net[0] — сеть, net[-1] — broadcast, net[1] — мин. ПК, net[-2] — макс. ПК; net.netmask — маска; net.num_addresses — всего адресов; -2 — число узлов.' },
    ],
    videos: [],
  },

  ege_geometry: {
    intro: 'В задаче 27 про звёзды нужно расстояние между точками — теорема Пифагора. Функции dist и centr — обязательный шаблон.',
    blocks: [
      { h: 'Расстояние', t: 'd = √((x2-x1)² + (y2-y1)²). Своя функция или встроенная math.dist.',
        code: 'from math import dist\nprint(dist([0, 0], [3, 4]))', out: '5.0' },
      { h: 'Своя dist', t: 'На случай, если math.dist нельзя — пишут руками.',
        code: 'def dist(a, b):\n    return ((b[0]-a[0])**2 + (b[1]-a[1])**2) ** 0.5\nprint(dist([0, 0], [3, 4]))', out: '5.0' },
      { h: 'Центр кластера (медоид)', t: 'Точка с минимальной суммой расстояний до всех остальных точек кластера.' },
    ],
    videos: [],
  },

  ege_turtle: {
    intro: 'Задача 6 — про исполнителя Черепаху. Python рисует фигуру, засыпает поле точками сетки, а ты считаешь точки внутри.',
    blocks: [
      { h: 'Движение', t: 'fd(n) — вперёд, bk(n) — назад, rt(угол) — поворот направо, lt(угол) — налево. Углы в градусах.' },
      { h: 'Перо', t: 'up() — поднять (двигаться без рисунка), down() — опустить. goto(x, y) — переместиться.' },
      { h: 'Скорость', t: 'tracer(0) отключает анимацию, update() показывает результат мгновенно.' },
      { h: 'Приём решения', t: '1) Рисуем фигуру по алгоритму. 2) Поднимаем перо, ставим dot(3,"red") в узлах сетки двойным циклом. 3) Считаем точки внутри фигуры глазами.' },
    ],
    videos: [],
  },

  ege_algo: {
    intro: 'Типовые приёмы ЕГЭ: работа с цифрами числа, поиск максимума, подсчёт по условию.',
    blocks: [
      { h: 'Сумма цифр числа', t: 'n % 10 — последняя цифра, n // 10 — убрать её. Повторяем в цикле.',
        code: 'n = 253\ns = 0\nwhile n > 0:\n    s = s + n % 10\n    n = n // 10\nprint(s)', out: '10' },
      { h: 'Поиск максимума', t: 'Заводим m = первый элемент, идём по списку и обновляем.',
        code: 'a = [3, 7, 2, 9, 4]\nm = a[0]\nfor x in a:\n    if x > m:\n        m = x\nprint(m)', out: '9' },
      { h: 'Подсчёт по условию', t: 'Счётчик k увеличиваем, когда элемент подходит.',
        code: 'a = [3, 8, 5, 9, 2, 7]\nk = 0\nfor x in a:\n    if x > 5:\n        k = k + 1\nprint(k)', out: '3' },
      { h: 'Количество делителей', t: 'Перебираем делители от 1 до n, считаем те, что делят нацело.',
        code: 'n = 12\nk = 0\nfor d in range(1, n + 1):\n    if n % d == 0:\n        k = k + 1\nprint(k)', out: '6' },
    ],
    videos: [],  // отдельного ролика по задачам ЕГЭ в марафоне нет
  },
};

// ID плейлиста марафона Py.Go (для кнопки «весь марафон»)
export const PLAYLIST_ID = 'PLW_RLSKbMD2WTqdex-dCYfRM7JtUooaTe';

// Полный марафон Py.Go: 25 дней кода (по порядку)
export const MARATHON = [
  { day: 1,  id: 'E8XjM4JvtrI', title: 'Установка и первая программа', dur: '3:35' },
  { day: 2,  id: 'EwlgAUwV3BE', title: 'Вывод, типы данных и переменные', dur: '7:06' },
  { day: 3,  id: 'LTX9YjRabPE', title: 'Арифметика строк', dur: '7:16' },
  { day: 4,  id: 'Vn6nS4xBJlg', title: 'Арифметика чисел', dur: '7:53' },
  { day: 5,  id: 'jsRZrw8HIGQ', title: 'Условный оператор, операции сравнения', dur: '9:27' },
  { day: 6,  id: '04hmvtSbfSg', title: 'Составные условия, логический тип', dur: '8:25' },
  { day: 7,  id: '2wt6UGUFRGQ', title: 'Переменная цикла FOR', dur: '7:07' },
  { day: 8,  id: 'iMCjAytQWmk', title: 'Варианты цикла FOR', dur: '7:17' },
  { day: 9,  id: 'XGgCn95Qi90', title: 'Цикл WHILE', dur: '8:40' },
  { day: 10, id: 'DLHAesA64Ow', title: 'Индексы строк', dur: '7:27' },
  { day: 11, id: 'wCPKfkLrYRg', title: 'Срезы строк', dur: '9:43' },
  { day: 12, id: 'A4pqJe8byTg', title: 'Сравнение строк', dur: '7:10' },
  { day: 13, id: '-tS7GAZbx44', title: 'Методы строк', dur: '9:35' },
  { day: 14, id: 'sEFm3hKv5Gc', title: 'Вещественные числа и тип float', dur: '7:49' },
  { day: 15, id: 'dci0W14OfPk', title: 'Массивы и основные операции с ними', dur: '4:36' },
  { day: 16, id: 'tcm9jWdp-6o', title: 'Добавление элементов в массив', dur: '10:48' },
  { day: 17, id: '8FYwxmYq2jk', title: 'Индексы элементов, изменение массива, срезы', dur: '7:27' },
  { day: 18, id: '9MX_eeIy1mI', title: 'Два типа циклов по массиву', dur: '9:33' },
  { day: 19, id: 'OrYbn1b5CW0', title: 'Методы join() и split()', dur: '6:40' },
  { day: 20, id: '97dOQ9pDSyY', title: 'Методы index(), count(), pop(), remove(), insert()', dur: '9:29' },
  { day: 21, id: 'pEqNSdOKRm4', title: 'Встроенные операторы и функции: in, not in, max, min', dur: '7:09' },
  { day: 22, id: 'BSJHrx1JmJg', title: 'Функции, основные понятия. Параметры функции', dur: '5:40' },
  { day: 23, id: 'sH9_nKZCLdY', title: 'Значение функции. Оператор return', dur: '8:55' },
  { day: 24, id: 'GISy6STCYdQ', title: 'Изменяемые типы. Изменение массивов', dur: '4:33' },
  { day: 25, id: 'jLWEAQIOcmQ', title: 'Стандартная библиотека Python. Модули', dur: '5:59' },
];
