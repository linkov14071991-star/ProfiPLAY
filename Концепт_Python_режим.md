# «Профик и Цифровая Вселенная» — игровой режим Python для Профик Арены

**Полный дизайн-документ**. Педагогическая база: программа Python для ЕГЭ по информатике (курс «Профиматика», 15 глав, задачи 2/6/8/9/13/14/15/16/17/19-21/23/24/25/26/27). Игровой референс: Duolingo + Human Resource Machine + Super Mario + CodeCombat, адаптировано под Telegram Mini App.

---

## 1. Общая концепция

### 1.1. Название режима

Внутри Профик Арены — режим называется **«Цифровая Вселенная»** (иконка 🐍, находится в разделе «🎯 Тренировки соло»). Слоган: **«Помоги Профику восстановить компьютерный мир — командой за командой».**

### 1.2. Сюжет одним абзацем

Однажды **Цифровая Вселенная** — мир, где живёт кот-айтишник Профик — начала разваливаться. Пропали алгоритмы, стёрлись переменные, циклы забыли, как крутиться, функции потеряли смысл. Профик обнаружил, что это дело рук **Багов** — маленьких злобных существ, которые едят код. Единственный способ спасти Вселенную — восстановить каждую утерянную конструкцию Python, которая, как оказалось, ещё и нужна для ЕГЭ. Игрок берёт на себя роль **Стажёра-программиста**, помогает Профику пройти все 15 глав программы и в финале сдаёт **Испытание Стобалльника** — итоговую миссию, которая моделирует все типы задач ЕГЭ по информатике.

### 1.3. Тон и настроение

- Разговорный, дружеский, с шутками уровня хорошего препода.
- Никаких «нажми зелёную кнопку далее» — весь текст в стиле Профика («Смотри, что получилось!», «Хмм, кажется тут баг…», «Это же прям как в задаче 17 на ЕГЭ!»)
- Ощущение «мы вместе с Профиком чиним мир», а не «одинокий ученик решает тесты».

### 1.4. Ключевой принцип

**Ученик не читает теорию — он играет.** Каждая новая идея вводится через action:

1. Игрок сталкивается с проблемой.
2. Пробует решить интуитивно.
3. Ошибается — получает мгновенную визуальную обратную связь.
4. Игра показывает правильный путь ОДНИМ жестом.
5. Игрок повторяет — уже правильно.
6. Через 3-4 уровня знание закрепляется, появляется новая тема.

---

## 2. Полная сюжетная линия

### Пролог. «Багопад»

Профик работает за ноутбуком. Экран моргает — приходит «Bug Wave», код рассыпается на буквы. Профик спасает единственный уцелевший файл `hello.py`. Обращается к игроку: «Помоги мне восстановить Вселенную. Начнём с самого главного — с первой команды».

### 15 глав = 15 регионов Цифровой Вселенной

Каждый регион — это визуально другой фон и своя тема. Прогресс по карте виден: пройденные регионы «оживают» (заполняются цветом, добавляется анимация), запертые — серые.

| Глава ЕГЭ | Игровой регион | Тема |
|---|---|---|
| 1. Ввод/вывод, арифметика | **Порт Первого Слова** | print, input, +, -, *, /, //, %, ** |
| 2. Ветвление | **Развилка Судьбы** | if / elif / else, and, or, not, == |
| 3. Цикл for | **Кольцо Повторов** | for, range(a,b,step), обратный ход |
| 4. Цикл while | **Башня Условий** | while, зацикливание, перевод СС |
| 5. Строки: индексы, срезы | **Библиотека Слов** | s[i], s[a:b], s[::-1], len(s) |
| 6. Строки: методы | **Мастерская Слов** | count, find, replace, upper/lower, split/join, f-strings, форматы |
| 7. Вещественные числа | **Гладь Дробей** | ceil, floor, round, int, деление |
| 8. Массивы | **Полка Списков** | [1,2,3], append, len, `[0]*n`, 2D-массивы |
| 9. Линейные алгоритмы | **Долина Счётчика** | счётчик, накопитель, поиск по условию |
| 10. Методы списков + генераторы | **Кузница Данных** | sort, count, `[int(x) for x in s]`, генераторы с фильтром |
| 11. Файлы | **Архив Диска** | open, 3 шаблона чтения, .readline() |
| 12. Словари | **Записная Книжка** | dict, .get, .items, подсчёт частот |
| 13. Функции и рекурсия | **Замок Функций** | def, return, параметры, рекурсия, базовый случай |
| 14. Сложность | **Ущелье Скорости** | O(n) vs O(n²), скользящее окно, len/count |
| 15. Библиотеки для ЕГЭ | **Космический Хаб** | math, itertools, functools, ipaddress, re, fnmatch, turtle, dist |

После 15-й главы — **Финальный Босс: Испытание Стобалльника**.

### Финал

Профик вручает игроку **сертификат «Стобалльник Python»** — виртуальную грамоту с уникальным QR-кодом (для шаринга в чат класса). В канале @profimatika_inf публикуется пост «Ученик N прошёл всю Цифровую Вселенную!». Игрок разблокирует **Prestige-режим** (все уровни заново, но с рандомизацией и ежедневными челленджами).

---

## 3. Структура всех глав

Каждая глава состоит из:

- **8–12 уровней** (в зависимости от объёма темы) по 30–90 секунд каждый
- **1 мини-босс** в середине главы (закрепление + чуть сложнее)
- **1 финальная миссия главы** (комплексное задание, использующее всё изученное)
- **Разблокировка новой темы** и открытие следующего региона на карте

### Детальный разбор глав

Дальше по каждой главе — название, идея, навык, механика, вид уровня, типы ошибок, закрепление, мини-босс, финал.

#### Глава 1. Порт Первого Слова

- **Идея.** Профик учит игрока «говорить» на языке машины. Порт с кораблями, каждый корабль — это одна команда, отправленная в консоль.
- **Навык.** `print()`, `input()`, арифметика: `+ - * / // % **`, порядок действий, скобки.
- **Механики уровней:**
  1. **«Отправь сообщение»** — 3 кнопки-варианта `print("A")`, `print"A"`, `print(A)`. Нужно выбрать синтаксически правильную. Ошибка → анимация «корабль тонет».
  2. **«Собери калькулятор»** — 5 кубиков `17`, `+`, `5`, `print`, скобки. Нужно перетащить в правильный порядок. Ошибка → неправильный результат в чате консоли.
  3. **«Предскажи вывод»** — показан код, 4 варианта ответа: `print(10 // 3)` → 3 / 3.33 / 4 / 0. Ошибка → анимация выполнения (10 распадается на 3 группы по 3).
- **Ошибки и лечение.**
  - Кавычки внутри `print` → ошибка «текст, а не переменная».
  - Путают `/` и `//` → анимация «одна черта = дробь, две = целое».
  - Забывают скобки в `print` → баг съедает вывод.
- **Закрепление.** После 4-го уровня — «Порт-Босс»: собери 3 print с разной арифметикой за 60 секунд. Награда: значок **⚓ Первое Слово**.
- **Финал главы.** «Отправь письмо в ЕГЭ» — калькулятор объёма памяти картинки (по задаче 7): игроку даётся 4 кубика (пиксели × биты / 8 / 1024) и `ceil`. Нужно собрать в правильном порядке. Награда: **+50 XP** + значок **📮 Почтальон Порта**.

#### Глава 2. Развилка Судьбы

- **Идея.** Профик стоит на развилке. Если условие верное — идём налево, если нет — направо. Каждый уровень — новая развилка с всё более сложными условиями.
- **Навык.** `if / elif / else`, операторы сравнения `==`, `<`, `>`, `<=`, `>=`, `!=`, логические `and`, `or`, `not`, вложенные условия.
- **Механики уровней:**
  1. **«Проведи Профика»** — на экране развилка со знаками (`x > 5?`). Игрок тапает налево или направо в зависимости от значения `x`.
  2. **«Расставь блоки»** — дан код с 3 пропусками. Нужно перетащить `if`, `elif`, `else` в правильные места.
  3. **«Найди баг»** — код с ошибкой `if x = 5:` (одно равно вместо двух). Тап по багу.
  4. **«Собери лабиринт»** — 3 вложенных условия. Игрок расставляет их так, чтобы «шарик» скатывался в нужное окно.
- **Ошибки и лечение.**
  - `=` вместо `==` → красная подсветка знака, всплывающая подсказка «одно = присваивание, два == сравнение».
  - Забыли двоеточие → синяя подсветка конца строки.
  - Забыли отступ → визуально код вылезает за границу «трубы if».
- **Мини-босс: Триада Условий.** Дан код с 3-мя `if` подряд без `elif`. Игрок должен переделать в `elif` иначе Триада съедает ответ.
- **Финал главы.** «Оценка ЕГЭ»: собрать `if/elif/elif/else` для оценок «отлично/хорошо/удовлетворительно/пересдача» из главы 2. Тестируется на 5 разных баллах. Награда: значок **🚦 Регулировщик**.

#### Глава 3. Кольцо Повторов (for)

- **Идея.** Кольцо, которое крутится. На каждом обороте Профик делает шаг вперёд. Игрок настраивает, сколько оборотов сделать и как.
- **Навык.** `for i in range(n)`, `range(a, b)`, `range(a, b, step)`, обратный ход `range(b, a, -1)`, перебор списка/строки `for x in list`.
- **Механики:**
  1. **«Заведи кольцо»** — выбери правильный `range()` из 4 вариантов, чтобы Профик прошёл ровно 5 шагов.
  2. **«Собери счётчик»** — конструктор `range(?, ?, ?)`. Дан результат (например, `1, 3, 5, 7, 9`), нужно подобрать параметры.
  3. **«Обратный ход»** — Профик должен пройти лестницу сверху вниз. Нужен `range(10, 0, -1)`.
  4. **«По значениям vs по индексам»** — предсказать вывод. `for x in [10,20,30]: print(x)` даст 10,20,30. А `for i in range(len(a)): print(i)` даст 0,1,2. Игрок выбирает верный.
- **Ошибки и лечение.**
  - Правая граница `range(1, 10)` = 1..9 (не 10!) → анимация «10 стоит за забором».
  - Отрицательный шаг с неправильными границами → «кольцо крутится в пустоту».
- **Мини-босс: Змея Диапазона.** Змея из 5 колец, у каждого свой `range`. Нужно расставить их так, чтобы вместе они прошли числа 1..20 без пропусков и повторов.
- **Финал главы.** «Задача 25»: перебор всех чисел в диапазоне и вывод тех, что делятся на 2025. Игрок собирает цикл, условие внутри и `print`. Награда: значок **🔄 Хозяин Цикла**.

#### Глава 4. Башня Условий (while)

- **Идея.** Башня, которая рушится по этажу за раз, пока условие верно. Профик должен управлять разрушением, чтобы не остаться под завалом (бесконечный цикл).
- **Навык.** `while условие`, обязательное изменение переменной, перевод в системы счисления через `while + % + //`.
- **Механики:**
  1. **«Разруши башню»** — задан начальный `n = 10`. Нужно расставить блоки: `while n > 0:` + `print(n)` + `n = n - 1`. Если забыл третий блок — «башня заваливает» экран (бесконечный цикл, экран красный, надо перезапустить).
  2. **«Переведи в двоичную»** — дано число, нужно собрать классический шаблон с `while`, `%`, `//`, `str(...)+s`.
  3. **«Найди зациклку»** — код без `n -= 1`. Тап по строке, где не хватает изменения.
- **Ошибки и лечение.**
  - Забыли менять переменную → таймер в углу (5 секунд), потом «баг сожрал память». Анимация зависания.
  - Поменяли порядок: сначала `n // 8`, потом остаток → остаток становится 0. Показ пошагового прогона.
- **Мини-босс: Вечный Цикл.** На экране код с потенциальной бесконечностью. Игрок должен найти и вставить условие выхода `break` или изменение переменной за 20 секунд.
- **Финал главы.** «Переведи 100 в восьмеричную» — собрать полный шаблон. Результат = 144. Награда: **♾ Мастер Циклов**.

#### Глава 5. Библиотека Слов (индексы и срезы)

- **Идея.** Библиотека, где каждая книга — это строка. У каждого символа есть номер (индекс), можно вырезать целые фразы (срезы).
- **Навык.** `s[i]`, отрицательные индексы `s[-1]`, срезы `s[a:b]`, `s[:b]`, `s[a:]`, шаг `s[::-1]`, `len(s)`.
- **Механики:**
  1. **«Вытяни символ»** — строка «ЕГЭ2026», нужно вытащить `s[3]` → «2».
  2. **«Собери слово»** — дано «Профиматика», нужно с помощью среза получить «фиматика» (`s[2:]`).
  3. **«Переверни книгу»** — из «алгоритм» получить «мтироглА» (`s[::-1]`).
  4. **«Найди букву»** — 5 карточек с индексами. Игрок ставит правильную под каждый символ.
- **Ошибки и лечение.**
  - Считают с 1 вместо 0 → анимация «первый вагон — это номер 0».
  - В срезе включают правую границу → «за забором».
- **Мини-босс: Загадка Дракона.** Дан код `s = "ПРОФИК"; print(s[1:4:2])`. Игрок должен предсказать вывод. Ответ: `РФ` (индексы 1 и 3, шаг 2).
- **Финал главы.** «Проверь окончание»: код проверяет, оканчивается ли слово на гласную через `s[-1] in 'АЕИОУЫЭЮЯЁ'`. Награда: **📖 Хранитель Слов**.

#### Глава 6. Мастерская Слов (методы + f-strings)

- **Идея.** Мастерская, где строки обрабатываются готовыми инструментами. У каждого метода — свой станок.
- **Навык.** `.count()`, `.find()`, `.replace()`, `.upper()/.lower()`, `.strip()`, `.split()`, `.startswith()/.endswith()`, `.isdigit()`, `+`, `*`, `in`. F-строки: `f"{x}"`, `f"{x:08b}"`, `f"{x:.2f}"`, `f"{x:032b}"`, `f"{x:x}"`, `f"{x:o}"`.
- **Механики:**
  1. **«Выбери инструмент»** — задача «переведи в верхний регистр», 4 метода на выбор.
  2. **«Собери f-строку»** — заготовка `f"{...}"`, нужно вставить `x:08b`. Ученик тапает «::08b» и попадает в фигурные скобки.
  3. **«Сколько раз?»** — строка «abracadabra», выбери количество вхождений «a». Предсказание.
  4. **«Раздели по пробелам»** — конструктор с `.split()` vs `.strip()`.
- **Ошибки и лечение.**
  - `.count("aa")` в «aaa» = 1 (не 2) → анимация «непересекающиеся» окна.
  - Забыли `f` перед строкой → фигурные скобки в выводе.
  - `s.upper()` не меняет `s` → анимация «результат надо сохранить».
- **Мини-босс: Хамелеон.** Дан код с 4 методами (`.upper().strip().replace(...).split()`) — игрок предсказывает финальный результат.
- **Финал главы.** «IP-адрес в двоичной»: собрать `f'{int(ip):032b}'`. Награда: **🔧 Мастер Форматов**.

#### Глава 7. Гладь Дробей (вещественные числа)

- **Идея.** Профик плавает по глади, надо аккуратно округлять — вверх, вниз, к ближайшему.
- **Навык.** `ceil`, `floor`, `round`, `int`, деление `/` даёт float, `from math import ceil, floor`.
- **Механики:**
  1. **«Округли»** — число 3.6, 3 кнопки: `ceil` (4), `floor` (3), `int` (3). Игрок выбирает ту, что просит задача («вверх для памяти»).
  2. **«Ошибка round»** — `round(2.5)` даёт 2 (банковское). Предсказание с ловушкой.
  3. **«Импорт»** — расставь `from math import` в нужное место кода.
- **Ошибки и лечение.**
  - Забыли импорт → красное подчёркивание «name is not defined».
  - Использовали `round` там, где нужен `ceil` → анимация «памяти не хватило».
- **Мини-босс: Точка Округления.** 5 подряд задач на разные типы округления.
- **Финал главы.** «Задача 11»: сколько бит на символ, если алфавит N=17. Ответ `ceil(log2(17))` = 5. Награда: **📐 Точный Профик**.

#### Глава 8. Полка Списков (массивы)

- **Идея.** Полка с ячейками. В каждую можно положить число, вытащить, посчитать сумму, найти максимум.
- **Навык.** `[1,2,3]`, `a[i]`, `len(a)`, `a.append(x)`, `[0]*n`, `max/min/sum(a)`, двумерные списки.
- **Механики:**
  1. **«Собери полку»** — конструктор списка из чисел через кнопки-числа.
  2. **«Индексы»** — на полке 5 ячеек, вытащи `a[2]` и `a[-1]`.
  3. **«2D-полка»** — таблица 3×3. Тап по `m[1][2]` подсвечивает клетку.
  4. **«Опасность копии»** — сравнение `[[0]*3]*3` vs `[[0]*3 for _ in range(3)]`. Игроку показывают, как в первом варианте все строки меняются вместе — «магия ссылок».
- **Ошибки и лечение.**
  - `[[0]*3]*3` при изменении одного элемента меняет все → анимация «связанные строки».
  - `.append` vs `+` → одна кладёт элемент, другая склеивает.
- **Мини-босс: Хранитель Полки.** 5 действий подряд: создай список, добавь 3 элемента, найди max, замени по индексу, посчитай sum.
- **Финал главы.** «Задача 9»: массив чисел, проверить `sum(a) > max(a)*2`. Награда: **📚 Библиотекарь**.

#### Глава 9. Долина Счётчика (линейные алгоритмы)

- **Идея.** Долина, по которой Профик идёт с блокнотом и считает всё подходящее — например, красные яблоки.
- **Навык.** Счётчик `count += 1`, накопитель `summa += x`, поиск максимума с условием, работа с индексами и значениями.
- **Механики:**
  1. **«Обнули счётчик»** — код с забытой строкой `count = 0`. Игрок тап на «баг».
  2. **«Считай подходящих»** — визуализация: список фигур, игрок ставит счётчик, шаг за шагом видит как счётчик растёт.
  3. **«Соседи»** — задача про пары соседних элементов. Правильный `range(len(a)-1)`.
- **Ошибки и лечение.**
  - Забыли `count = 0` → счётчик стартует с мусором, анимация.
  - Вышли за границу `a[i+1]` при `i = len(a)-1` → «упал с полки».
- **Мини-босс: Демон Границ.** Задача с массивом и парами соседей. Игрок должен указать правильный `range`.
- **Финал главы.** «Задача 9 в лоб»: посчитать числа, удовлетворяющие условию + сумма. Награда: **✍ Летописец**.

#### Глава 10. Кузница Данных (методы списков + генераторы)

- **Идея.** Кузница, где сырые данные (строки) превращаются в готовые (списки чисел) одним ударом молота (`.split()` + `[int(x) for ...]`).
- **Навык.** `.sort()`, `sorted()`, `.count()`, `.index()`, генераторы `[x for x in ...]`, генераторы с фильтром `[x for x in ... if ...]`, чтение чисел через `.split()` + `int`.
- **Механики:**
  1. **«Сортируй»** — визуально: полка неотсортирована, тап `.sort()` — карточки красиво прыгают в порядок.
  2. **«Собери генератор»** — конструктор `[?  for ? in ?]`. Нужно расставить 3 части в правильный порядок.
  3. **«Фильтр»** — добавь `if x % 2 == 0` в конец генератора, чтобы получить только чётные.
  4. **«int() спасает»** — задача сравнить строки vs числа. Показ: `"10" < "2"` даёт True! Ученик добавляет `int(x)`.
- **Ошибки и лечение.**
  - `a = a.sort()` даёт `None` — визуализация «данные исчезли».
  - Забыли `int()` → показывают алфавитное сравнение строк.
- **Мини-босс: Форгемастер.** Собрать генератор из 5 частей за 60 секунд: чтение файла → split → int → фильтр → сортировка.
- **Финал главы.** «Задача 17»: 3 строки кода — прочитать файл, отсортировать, вывести max. Награда: **⚒ Кузнец Кода**.

#### Глава 11. Архив Диска (файлы)

- **Идея.** Архив, где файлы — свитки. Профик учится их разворачивать (3 шаблона чтения).
- **Навык.** `open()`, `[int(x) for x in f]`, `f.readline()`, `for line in f`, `line.split()`, `.strip()`.
- **Механики:**
  1. **«Выбери шаблон»** — 3 варианта данных (числа в столбик / числа через пробел / одна гигантская строка). Игрок выбирает подходящий шаблон.
  2. **«Собери чтение»** — конструктор из `open`, `for`, `int`, `split`.
  3. **«Строка vs число»** — задача с ловушкой: без `int()` не работает `sum`.
- **Ошибки и лечение.**
  - Не преобразовали `int()` → все ошибки, анимация с алфавитным сравнением.
  - Забыли `.strip()` → лишний `\n` в конце строки.
- **Мини-босс: Смотритель Архива.** 3 разных файла подряд, каждый требует свой шаблон.
- **Финал главы.** «Задача 24»: гигантская строка, найти длину. Профик разворачивает свиток на 100000 символов. Награда: **📜 Хранитель Архива**.

#### Глава 12. Записная Книжка (словари)

- **Идея.** Профик ведёт записную книжку: имя → телефон, буква → счётчик. Не по номеру, а по ключу.
- **Навык.** `d = {}`, `d[k] = v`, `d[k]`, `d.get(k, 0)`, `k in d`, `d.items()`, подсчёт частот.
- **Механики:**
  1. **«Заведи запись»** — тап по ключу «Аня», ввод значения. Как визуальная карточка.
  2. **«Собери счётчик частот»** — код с `d[ch] = d.get(ch, 0) + 1` собирается из 3 блоков.
  3. **«Ключ или ошибка?»** — `d["нет"]` → красный экран, замена на `d.get("нет", 0)` → зелёный.
- **Ошибки и лечение.**
  - `d["нет"]` без .get → KeyError, красная анимация.
  - Присваивание по существующему ключу затирает старое → предупреждение.
- **Мини-босс: Библиограф.** Посчитать частоты букв в строке «Профиматика» через словарь.
- **Финал главы.** Подсчёт частот для задачи ЕГЭ (упрощённо). Награда: **📔 Хранитель Записей**.

#### Глава 13. Замок Функций

- **Идея.** Замок с башнями. Каждая башня — функция. Можно построить свою и вызвать сколько угодно раз.
- **Навык.** `def имя(параметры): return ...`, вызовы, рекурсия, базовый случай, `F(n) = f(F(n-1))`.
- **Механики:**
  1. **«Построй функцию»** — конструктор `def`, `(x)`, `:`, `return`, `x*x`. Правильный порядок.
  2. **«Матрёшка»** — визуализация рекурсии `F(5) → F(4) → ... → F(1) → return 1 → назад умножения`.
  3. **«Задача 16 в лоб»** — дана формула `F(n) = n + F(n-2)`, переписать буквально.
  4. **«Найди базовый»** — код без базового случая. Через 3 секунды — «RecursionError», игрок должен вставить `if n <= 1: return 1`.
- **Ошибки и лечение.**
  - Забыли `return` → функция возвращает `None`, анимация «пустой шар».
  - Забыли базовый случай → стек ошибок, «замок рушится».
- **Мини-босс: Дракон Рекурсии.** Собрать `F(n) = n * F(n-1)` с базовым случаем.
- **Финал главы.** Посчитать F(5) для F(n)=n+F(n-2), n>0, иначе 0. Ответ: 30. Награда: **🏰 Владыка Замка**.

#### Глава 14. Ущелье Скорости (сложность)

- **Идея.** Профик бежит по ущелью, и время его гонит. Есть быстрый путь (O(n)) и медленный (O(n²)) — надо выбрать правильный.
- **Навык.** Понимание O(n) vs O(n²), скользящее окно, замена вложенных циклов, использование словарей для быстрого доступа.
- **Механики:**
  1. **«Быстрый или медленный?»** — 2 варианта кода на одну задачу, игрок выбирает O(n) по визуальному темпу.
  2. **«Скользящее окно»** — интерактивный слайдер по строке, показывает как «окно» едет по данным. Игрок понимает интуитивно.
  3. **«Замени вложенный цикл»** — код с двойным `for`, задача переписать через один.
- **Ошибки и лечение.**
  - Два вложенных цикла на большом массиве → таймер зашкаливает, «время истекло».
- **Мини-босс: Часы Судьбы.** Задача 24 в упрощении: найти макс. длину окна с подстрокой «2025» ≤ 90 раз. Игрок собирает шаблон.
- **Финал главы.** Скользящее окно на строке 10000 символов, укладывается в лимит. Награда: **⚡ Быстрый Профик**.

#### Глава 15. Космический Хаб (библиотеки)

- **Идея.** Космическая станция, каждый терминал — модуль. Профик подключает нужные и решает задачи ЕГЭ.
- **Навык.** `math` (ceil, floor, sqrt, gcd, log2, factorial, pi, dist), `itertools` (product, permutations, combinations), `functools` (lru_cache), `ipaddress`, `re` (finditer, findall, жадные/ленивые), `fnmatch`, `turtle`, кастомная `dist` для задачи 27.
- **Механики:**
  1. **«Импорт-квест»** — задача просит `ceil`, игрок выбирает правильный `import`.
  2. **«Комбинаторика»** — задача 8, `product('АБ', repeat=3)`. Игрок выбирает правильный инструмент из itertools.
  3. **«Регулярка»** — конструктор `r'[0-9]+'` из символьных классов и квантификаторов.
  4. **«fnmatch»** — маска `"1?2*7"`, выбор из чисел, какое подходит.
  5. **«Turtle»** — маленький визуализатор, Профик рисует фигуру по коду `fd, rt, lt`. Игрок предсказывает, что получится.
  6. **«lru_cache»** — код с медленной рекурсией. Игрок добавляет `@lru_cache(10000)` — счётчик времени зелёный.
- **Мини-боссы:** каждая библиотека — отдельный мини-босс.
- **Финал главы.** «Задача 27 (упрощённо)»: посчитать `dist([0,0], [3,4])` — 5.0. Награда: **🌌 Космонавт**.

---

## 4. Структура уровней (полная)

**Всего уровней: ~150** (15 глав × ~10 уровней). Плюс мини-боссы (15) и финалы глав (15) и Итоговый босс.

- **Микро-уровни (первые 3 в каждой главе)**: 15–30 секунд. Одна механика, одна ошибка, одна победа.
- **Средние уровни (4–7)**: 30–60 секунд. Комбинация 2 механик.
- **Продвинутые (8–10)**: 60–90 секунд. Полный код из 3–5 частей.
- **Мини-босс главы**: 60–120 секунд. Комплексное задание.
- **Финал главы**: до 3 минут. Задача ЕГЭ в упрощённой форме.

### Пример прогрессии внутри главы (Глава 3, for):

| # | Название | Механика | Навык |
|---|---|---|---|
| 1 | Первый оборот | Тап на `range(5)` из 3 | что такое for |
| 2 | Пять шагов | Выбор из 4 вариантов range | правая граница не входит |
| 3 | С шагом | Конструктор `range(a, b, step)` | step |
| 4 | Обратный ход | Профик спускается | отрицательный шаг |
| 5 | По списку | `for x in [10,20,30]` | значения vs индексы |
| 6 | По строке | `for c in "ЕГЭ"` | строка тоже итерируется |
| 7 | Индексы | `for i in range(len(a))` | когда нужны соседи |
| 8 | Мини-босс: Змея | 5 range склеить | комбо |
| 9 | Финал: Задача 25 | Перебор + условие | ЕГЭ |

---

## 5. Игровые механики (расширенный каталог)

Всего **18 базовых механик**, каждая подходит для определённых тем.

1. **Предскажи вывод** — код + 4 варианта. Учит читать код.
2. **Найди баг** — тап по строке с ошибкой. Учит внимательности.
3. **Собери код** (drag & drop) — 5 блоков, расставь в правильный порядок. Учит структуре программы.
4. **Дополни строку** — введи недостающий оператор. Учит синтаксису.
5. **Выбери оператор** — 4 варианта: `//`, `/`, `%`, `**`. Учит различать.
6. **Расставь отступы** — вертикальная линия, куда «прицепить» строку. Учит блокам.
7. **Соедини переменные** — соединение линиями `x` с `x`, `y` с `y`. Учит связям.
8. **Симулятор выполнения** — пошаговый прогон, кнопка «дальше». Учит логике.
9. **Визуализация памяти** — квадратики-ячейки, обновление значения. Учит переменным.
10. **Визуализация цикла** — «глазок» перебирает элементы. Учит for.
11. **Визуализация условия** — развилка со стрелкой. Учит if.
12. **Визуализация рекурсии** — матрёшка, вложенные вызовы. Учит стек.
13. **Собери генератор** — 3 части `[..for..in..if..]`. Учит компактному синтаксису.
14. **Скользящее окно** — интерактивный слайдер по строке. Учит O(n).
15. **Конструктор f-строки** — формат внутри `{}`. Учит форматам.
16. **Turtle-художник** — вводишь fd/rt/lt, смотришь результат. Учит визуально.
17. **Импорт-квест** — какая библиотека для этой задачи? Учит модулям.
18. **Симулятор ЕГЭ** — типовая задача из блока задач. Финальное закрепление.

Каждая механика имеет **педагогическое обоснование**:

- «Найди баг» тренирует «глаз препода» — умение быстро сканировать код (нужно для задачи 22 «отладка», хотя её и не входит в наш список, но навык универсален).
- «Собери код» борется с фобией «не знаю с чего начать» — программа собирается из блоков, а не пишется с нуля.
- «Визуализация памяти/цикла» решает главную проблему новичков — они не понимают, что происходит внутри.
- «Скользящее окно» показывает интуитивно то, что словами объяснить сложно (сложность алгоритма).

---

## 6. Знания по уровням (полная таблица)

Каждая тема ЕГЭ покрыта конкретными уровнями. Ниже — таблица «тема → уровни».

| Тема ЕГЭ | Уровни в игре |
|---|---|
| `print()` | 1.1–1.3, повторяется во всех главах |
| `input()` | 1.4 (короткое знакомство, но не критично) |
| `int()`, `float()`, `bool()`, `str()` | 1.5–1.7, повторяется в 10 (при чтении из split) |
| переменные, присваивание | 1.2, 2.1, 3.1, 9.1 |
| арифметика `+ - * / // % **` | 1.6–1.10 |
| порядок действий | 1.8 |
| `if / elif / else` | 2.1–2.9 |
| `and`, `or`, `not` | 2.5–2.7 |
| вложенные условия | 2.8 |
| `while` | 4.1–4.9 |
| `for` | 3.1–3.9 |
| `range(a, b, step)` | 3.2–3.6 |
| `for x in list/str` | 3.5–3.7 |
| строки: `s[i]` | 5.1–5.3 |
| срезы `s[a:b]`, `s[::-1]` | 5.4–5.7 |
| `len(s)` | 5.8, повторяется |
| методы строк | 6.1–6.9 |
| f-строки, форматы `08b`, `.2f`, `x`, `o` | 6.6–6.9 |
| `ceil`, `floor`, `round`, `int` | 7.1–7.6 |
| списки `[1,2,3]`, `a[i]` | 8.1–8.3 |
| `append`, `pop`, `remove` | 8.4–8.6 |
| `[0]*n`, 2D-списки, генератор `[[0]*n for _ in range(m)]` | 8.7–8.9 |
| `max`, `min`, `sum`, `len` | 8.4, 9.1 |
| счётчик, накопитель | 9.1–9.7 |
| поиск по условию | 9.5–9.8 |
| `.sort()`, `sorted()` | 10.1–10.3 |
| генераторы списков `[x for x in ...]` | 10.4–10.8 |
| генераторы с `if` | 10.6 |
| `[int(x) for x in s.split()]` | 10.7 |
| файлы: `open`, 3 шаблона | 11.1–11.9 |
| `.readline()` | 11.7 |
| словари | 12.1–12.9 |
| функции `def`, `return`, параметры | 13.1–13.5 |
| рекурсия, базовый случай | 13.6–13.9 |
| сложность O(n) vs O(n²) | 14.1–14.4 |
| скользящее окно | 14.5–14.9 |
| `math` (все функции) | 15.1–15.4 |
| `itertools` | 15.5–15.6 |
| `functools.lru_cache` | 15.7 |
| `ipaddress` | 15.8 |
| `re` (finditer, findall, жадные/ленивые) | 15.9–15.11 |
| `fnmatch` | 15.12 |
| `turtle` | 15.13 |
| `math.dist` для задачи 27 | 15.14 |

**Все темы из файла ЕГЭ покрыты.** Ни одной не пропущено.

---

## 7. Закрепление и повторение

### Интервальное повторение

Каждый новый уровень должен использовать хотя бы одну старую тему. Формула:

- **Уровень n** учит новую тему + повторяет темы из глав n-1, n-2, случайную из n-3.
- Например, уровень «for + условие» из главы 3 использует `if` из главы 2 и `print` из главы 1.

### Ежедневный «Разогрев»

Каждый день Профик предлагает 3 короткие задачки на **уже пройденные темы** (~30 секунд каждая). Награда: XP + прогресс к ачивке.

### «Экзаменационная неделя»

Раз в неделю — комплексный уровень, где нужно применить темы из **всех пройденных глав**. Награда: значок и +200 XP.

### «Стена ошибок»

Если игрок много раз спотыкается на одной механике, игра **автоматически** подсовывает ему +2 уровня на эту тему в следующий заход. Это невидимая для игрока адаптация.

---

## 8. Игровые эмоции ученика

- **Удивление** («Оказывается, `[[0]*3]*3` это ловушка!»)
- **Понимание** («А, вот почему `range(1, 10)` даёт 9, а не 10!»)
- **Гордость** («Я собрал свою первую функцию!»)
- **Азарт** («Ещё один уровень, и открою значок Мидла!»)
- **Спокойствие** после победы над мини-боссом
- **Волнение** перед финалом главы
- **Ностальгия** от возврата в пройденные регионы (карта)

---

## 9. Награды в режиме Python

Полностью интегрируются с общей системой Профик Арены. Никакой отдельной валюты.

- **XP** за каждый уровень: 5 (микро) / 10 (средний) / 20 (продвинутый) / 40 (мини-босс) / 60 (финал главы) / 200 (финал игры).
- **Рейтинг**: за прохождение главы целиком +30 рейтинга. Отдельные уровни не дают рейтинг (иначе кап 100/день не даст пройти игру за месяц).
- **Значки глав** (см. финалы каждой главы) — коллекционные.
- **Ачивки Python** (см. следующий раздел) — добавляются к общим 27.
- **Титулы** в профиле: «🐍 Питонист» (после 5 глав), «🐍🐍 Опытный питонист» (после 10), «🐍🐍🐍 Мастер Python» (после всех 15 + финала).
- **Идеальное прохождение** (все правильно с первого раза): +50% XP за уровень.
- **Комбо**: 5 идеально пройденных подряд → +значок и мультипликатор.
- **Стрик Python**: отдельный подсчёт дней, играешь ли ты в Python-режим. При 7 днях подряд — +значок.

---

## 10. Ачивки Python (в дополнение к 27 общим)

- 🐣 **Первый print** — сыграл первый уровень
- ⚓ **Первое Слово** — прошёл главу 1
- 🚦 **Регулировщик** — прошёл главу 2
- 🔄 **Хозяин Цикла** — прошёл главу 3
- ♾ **Мастер Циклов** — прошёл главу 4
- 📖 **Хранитель Слов** — прошёл главу 5
- 🔧 **Мастер Форматов** — прошёл главу 6
- 📐 **Точный Профик** — прошёл главу 7
- 📚 **Библиотекарь** — прошёл главу 8
- ✍ **Летописец** — прошёл главу 9
- ⚒ **Кузнец Кода** — прошёл главу 10
- 📜 **Хранитель Архива** — прошёл главу 11
- 📔 **Хранитель Записей** — прошёл главу 12
- 🏰 **Владыка Замка** — прошёл главу 13
- ⚡ **Быстрый Профик** — прошёл главу 14
- 🌌 **Космонавт** — прошёл главу 15
- 💎 **Стобалльник** — прошёл Финального Босса
- 🎯 **Перфекционист** — все уровни главы 1 идеально
- 🔥 **Огонь Комбо** — 20 идеальных подряд
- 🐍 **Питонист** — прошёл 5 глав
- 🌟 **Секретный: Ночная Сова** — сыграл 5 уровней после полуночи
- 🎂 **Секретный: День Рождения Профика** — сыграл 1 июня
- 🏆 **Speedrun** — прошёл главу 1 меньше чем за 10 минут

Всего **23 ачивки** Python + общие 27 = **50 в системе**.

---

## 11. Полная система прогрессии

### Скорость прохождения

- Быстрое: 1 глава за 2–3 вечера, вся игра за 3 недели.
- Среднее: 1 глава в неделю, вся игра за 3 месяца.
- Медленное: 1 глава в 2 недели, вся игра за 6 месяцев.

### Прогресс отображается

- **Мировая карта** с 15 регионами. Пройденные — с цветом и анимацией. Текущий — с пульсирующей меткой Профика. Заблокированные — серые.
- **Полоска глав** сверху: 3/15 глав.
- **Кристаллы**: каждый уровень даёт кристалл (белый). Идеальное прохождение — синий. Комбо → золотой.

### Разблокировки

- **Глава 1** доступна сразу.
- Каждая следующая — после прохождения финала предыдущей.
- **Финальный Босс** — после всех 15 глав.
- **Prestige-режим** — после Финального Босса.

---

## 12. Полная система повторения

### Механизмы

1. **Инкрементальное вплетение**: новая тема + 1 старая тема (уровень 3.5 использует `if` из главы 2).
2. **Утренний разогрев**: 3 задачи в день на случайные пройденные темы.
3. **Еженедельные комплексные**: раз в 7 дней, задача из 3+ тем.
4. **Стена ошибок**: адаптивная система, подсовывает больше уровней на слабые места.
5. **Prestige**: пройденную игру можно проходить снова с рандомизацией — самые сложные уровни из всех глав вперемешку.

### Пример вплетения (уровень 6.3 «замени букву»)

Уровень главы 6 (методы строк), тема `.replace()`. Дополнительно используется:
- `print()` (глава 1)
- `if` (глава 2) для проверки результата
- `len()` (глава 5)
- Мини-квиз в конце: «А что делает `.strip()`?» (глава 6, но раньше в цепочке)

---

## 13. Баланс сложности

### Принцип «легко войти, тяжело выйти»

- Первые 10 уровней — 10-15 секунд каждый, ошибиться сложно.
- Уровни 11-30 — вводят новые темы, но всегда 1 новая + 1 знакомая.
- Уровни 31-80 — комбо: 2-3 темы вместе.
- Уровни 81-150 — уже настоящая программа из 5+ строк.
- Финалы глав — задачи ЕГЭ в упрощённой форме.
- Финальный Босс — всё сразу.

### Ошибки не наказывают

- Первая ошибка — подсказка от Профика.
- Вторая ошибка — показ правильного решения с подсветкой.
- Третья ошибка — уровень «раскладывается» на подшаги, каждый решаешь отдельно.
- **Провалов не бывает**. Только скорость прохождения.

### Скорость влияет на XP

- Прошёл с 0 ошибок за первый раз → +100% XP (идеальное прохождение).
- 1-2 ошибки → полный XP.
- 3+ ошибок → 70% XP + автоматически добавляется 1 уровень на повторение темы.

---

## 14. Примеры экранов интерфейса

### 14.1. Карта Цифровой Вселенной

Вертикальная карта, скроллится вниз. 15 регионов, соединены дорожкой. Профик стоит на текущем. Пройденные — с сияющим значком. Тап по региону — обзор + кнопка «В главу».

### 14.2. Уровень «Предскажи вывод»

- Сверху: заголовок «Уровень 3.4 — Обратный ход»
- Центр: блок кода (моноширинный, крупный, подсветка синтаксиса)
- Ниже: 4 кнопки-варианта с числами
- Снизу: полоска прогресса главы

### 14.3. Уровень «Собери код»

- Сверху: цель («Собери цикл: вывести 5, 4, 3, 2, 1»)
- Центр: 5 «магнитиков» с частями кода, случайно расположены
- Ниже: пустая рамка кода 5 строк, куда игрок перетаскивает
- Кнопка «Проверить»

### 14.4. Уровень «Найди баг»

- Сверху: цель («Профик написал код, но что-то не так»)
- Центр: код с одной ошибкой
- Игрок тапает по строке с багом
- Мгновенная анимация — правильная строка либо тонкая рамка с исправлением

### 14.5. Мини-босс

- Тёмный фон, силуэт «монстра» из букв (например, гигантский знак `=` для «Триады Условий»)
- 5 подзадач подряд, полоска HP монстра
- Успех = дым, свет, значок

### 14.6. Финальный экран главы

- Крупный значок главы (например, ⚓)
- «Ты прошёл главу 1! +50 XP · +30 рейтинга · разблокирован регион 2»
- Кнопка «Открыть следующую главу»

---

## 15. Примеры прохождения первых 30 уровней

Полное описание уровней 1.1 — 3.9.

### 1.1 «Первое слово»
- Показ: пустая консоль. Профик: «Скажи ей „Привет“».
- 3 кнопки: `print("Привет")`, `print(Привет)`, `печатать("Привет")`.
- Тап на первый — консоль печатает «Привет» + фейерверк.
- Тап на второй — консоль краснеет, Профик: «Кавычки забыл!». Повторный тап.
- Тап на третий — консоль краснеет, Профик: «На Python это будет `print`, не по-русски».
- **Учит:** синтаксис `print("...")`.

### 1.2 «Много слов»
- «Профик хочет, чтобы консоль сказала 3 разных слова». Дано 3 пустые команды `print(?)`. Игрок тапает и пишет.
- **Учит:** `print` можно вызывать много раз.

### 1.3 «Первая переменная»
- Профик показывает коробку. «Положи туда число». Игрок пишет `x = 5`. Потом `print(x)`. Коробка «излучает» 5 → в консоль.
- **Учит:** присваивание, `print(переменная)`.

### 1.4 «Ввод от игрока»
- «Спроси имя». Появляется `input()`, игрок вводит имя, Профик здоровается через `print("Привет, " + name)`.
- **Учит:** input, склейка строк.

### 1.5 «Первый пример»
- `print(2 + 3)` → 5. Игрок предсказывает.
- **Учит:** `+` в print.

### 1.6 «Делим и умножаем»
- 3 задачи подряд: `17 - 5`, `4 * 6`, `20 / 4`. Игрок предсказывает.
- **Учит:** `- * /`.

### 1.7 «Одна черта или две?»
- Ключевой уровень. `10 / 3` → 3.33... vs `10 // 3` → 3.
- Игрок сравнивает.
- **Учит:** различие `/` и `//`.

### 1.8 «Порядок действий»
- `2 + 3 * 4` → 14. Игрок предсказывает.
- Затем `(2 + 3) * 4` → 20. Скобки меняют всё.
- **Учит:** приоритет.

### 1.9 «Остаток и степень»
- `10 % 3` → 1. `2 ** 5` → 32.
- **Учит:** `%` и `**`.

### 1.10 Финал главы 1: Задача 7
- Профик: «На ЕГЭ хотят посчитать байты картинки 1024×768, 16 бит на пиксель. В Кбайтах, округли вверх».
- Игрок собирает: `ceil(1024 * 768 * 16 / 8 / 1024)`.
- Награда: значок ⚓.

### 2.1 «Развилка»
- Развилка со стрелкой. `x = 10; if x > 5: → налево`.
- Игрок выбирает правильную ветку.
- **Учит:** `if`.

### 2.2 «Иначе»
- Условие ложно. Появляется `else`. Игрок ведёт Профика направо.
- **Учит:** `if / else`.

### 2.3 «Три варианта»
- Число может быть >0, <0, =0. Игрок собирает `if / elif / else` из 3 блоков.
- **Учит:** `elif`.

### 2.4 «Двоеточие пропало»
- Код с одной ошибкой: `if x > 5` без `:`. Игрок ставит двоеточие.
- **Учит:** синтаксис.

### 2.5 «И»
- Задача: «выведи ‘от 1 до 99’, если x > 0 И x < 100». Игрок вставляет `and`.
- **Учит:** логика.

### 2.6 «ИЛИ»
- Задача: «выведи ‘выходной’, если день = сб ИЛИ вс». Игрок вставляет `or`.
- **Учит:** `or`.

### 2.7 «Не»
- «Выведи ‘не число’, если не является числом». `if not s.isdigit(): ...`.
- **Учит:** `not`.

### 2.8 «Вложенные»
- Задача: «если x > 0, то если x % 2 == 0, выведи чётное». Игрок вкладывает.
- **Учит:** вложенность.

### 2.9 Мини-босс: Триада Условий
- Дан код с 3 отдельными `if` — надо переделать в `elif` цепочку. Ошибка = 2 срабатывания одновременно.

### 2.10 Финал главы 2: Оценки
- Собрать `if ball >= 80: "отлично" elif ...`. Тест на 5 баллах.
- Награда: 🚦.

### 3.1 «Первый оборот»
- Профик крутится вокруг оси. `for i in range(5): print(i)` → 0,1,2,3,4.
- **Учит:** for + range.

### 3.2 «Правая граница»
- `range(1, 10)` даёт 1..9, не 10. Игрок ловит ловушку.
- **Учит:** правая граница не входит.

### 3.3 «Шаг»
- Задача: «выведи чётные до 10». Собрать `range(2, 11, 2)`.
- **Учит:** шаг.

### 3.4 «Обратный ход»
- Задача: «выведи 10, 8, 6, 4, 2». Собрать `range(10, 1, -2)`.
- **Учит:** отрицательный шаг.

### 3.5 «По списку»
- `for x in [10, 20, 30]: print(x)`. Предсказать вывод.
- **Учит:** `for x in list`.

### 3.6 «По строке»
- `for c in "ЕГЭ": print(c)` → Е / Г / Э.
- **Учит:** строка тоже итерируется.

### 3.7 «Значение vs индекс»
- Ловушка: `for x in [10,20,30]: print(x)` — правильно выводит значения. Но `for i in range(3): print([10,20,30][i])` — тоже работает. Показать разницу.
- **Учит:** когда `x`, когда `i`.

### 3.8 Мини-босс: Змея Диапазона
- 5 range склеить так, чтобы дать 1..20 без пропусков.

### 3.9 Финал главы 3: Задача 25
- Перебор `range(174457, 174505)`, вывести кратные 2025. Игрок собирает.
- Награда: 🔄.

---

## 16. Примеры сложных уровней (ближе к финалу)

### Уровень 13.7 (глава функций): «Функция в функции»
- Дана рекурсивная функция `F(n) = F(n-1) + F(n-2)`, базовые случаи.
- Игрок должен собрать её из 5 частей + добавить `@lru_cache`.

### Уровень 14.6 (сложность): «Скользящее окно»
- Дана гигантская строка (100000 символов). Задача: найти макс. длину подстроки, где «BC» встречается не более 3 раз.
- Игрок собирает: цикл по строке, счётчик `k`, отрезание слева `while k > 3`, `s = s[1:]`, `m = max(m, len(s))`.
- Наивное решение (два вложенных for) не пройдёт по времени — визуализация.

### Уровень 15.10 (регулярки): «Ленивая жадность»
- Дана строка `"<a><b><c>"`. Задача: получить `['<a>', '<b>', '<c>']`, а не `['<a><b><c>']`.
- Игрок исправляет `r'<.+>'` на `r'<.+?>'`.

### Уровень 15.13 (turtle): «Начерти квадрат»
- Игрок собирает: `for i in range(4): fd(100); rt(90)`. Профик рисует квадрат в окне визуализатора.

---

## 17. Финальное испытание — «Экзамен Стобалльника»

Разблокируется после прохождения всех 15 глав.

### Формат

**7 миссий подряд**, моделирующих типы задач ЕГЭ по Python:

1. **Миссия 2** — таблица истинности (циклы + условия).
2. **Миссия 8** — перебор слов с `itertools.product`.
3. **Миссия 9** — файл + генератор + условие.
4. **Миссия 14** — перевод в СС через `while + % + //`.
5. **Миссия 17** — файл + `sort` + условие + max.
6. **Миссия 24** — скользящее окно.
7. **Миссия 25** — `for` + `fnmatch` + маска.

### Прохождение

Каждая миссия — 5–10 минут. Без штрафов за время. Ошибок допускается не больше 3 на миссию. Больше 3 — «переигровка» этой миссии.

### Награда

- **💎 Стобалльник** — легендарная ачивка (единственная с бриллиантом).
- **+2000 XP** и **+300 рейтинга** — единоразово, обходит дневной кап (специальное исключение для Финала).
- Титул в профиле: **«🐍🐍🐍 Мастер Python»**.
- Виртуальный **сертификат** — красивая PNG, можно скачать и поделиться (аналог виральной карточки дуэли, но для достижений).
- Пост в @profimatika_inf: «Ученик N прошёл Цифровую Вселенную!»
- Разблокировка **Prestige-режима**.

### Prestige-режим

Проходишь всю игру заново, но:
- Уровни рандомизированы.
- Сложность выше (меньше подсказок, тайминги жёстче).
- Каждый уровень даёт бонусные очки к общему рейтингу.
- Ачивки Prestige: 🐍💎 «Дважды Стобалльник», 🐍💎💎 «Trice Legendary», 🐍💎💎💎 «Vseznajka».

---

## 18. Как режим интегрируется в существующую Профик Арену

**Без создания отдельной системы прогресса.** Всё через существующие механики:

- **XP** — обычный XP, идёт в общий уровень аккаунта.
- **Ачивки Python** — расширяют существующий каталог (было 27, станет 50).
- **Дневные квесты** — добавляется новый тип: «Пройди 3 уровня Python».
- **Стрик** — специальный «Стрик Python» отдельно, но общий стрик игры считается.
- **Профиль** — новый раздел «Python-прогресс»: сколько глав пройдено, % прохождения.
- **Лидерборд** — можно добавить отдельную вкладку «По Python» (сортировка по пройденным главам).
- **Мотивирующая строка на главной**: «Ещё 3 уровня Python до значка Мастер Форматов!»
- **Реплики Профика**: расширяются контексты «python_start», «python_win», «python_fail».

---

## 19. UX-детали для Telegram Mini App

- **Вертикальная ориентация**, крупные кнопки-магнитики (минимум 44px по обеим осям).
- **Максимум 1-2 клика** для действия.
- **Тап-области** большие, ошибиться промахом сложно.
- **Свайпы** — переход между уровнями, между главами.
- **Хаптик-фидбек** через Telegram HapticFeedback API — на каждое действие.
- **Автосохранение**: если игрок закрыл приложение посреди уровня — вернётся точно на то же место.
- **Оффлайн-режим**: базовые уровни (без файлов ЕГЭ) работают без интернета — данные синхронизируются при возвращении.
- **Никаких сложных 3D-график** — только SVG/CSS/JS. Профик — тот же SVG-маскот.
- **Один визуальный язык**: те же фиолетово-салатовые цвета, что и во всей Арене.

---

## 20. Оценка объёма разработки

Для реализации всей игры:

- **150 уровней × ~2 часа дизайна+кода = 300 часов**
- **База кода**: движок уровней (30 часов) + 18 механик (18 × 10 часов = 180 часов) + карта мира (20 часов) + прогрессия/бэкенд (40 часов) + анимации/полировка (40 часов) = **310 часов**
- **Итого: ~600 часов** = 3-4 месяца работы одного дизайнера-разработчика.

Реалистичный MVP:
- **1-я итерация**: Главы 1-3 (30 уровней) + карта + движок = 100 часов. Уже можно давать ученикам.
- **2-я итерация**: Главы 4-8 (50 уровней) = 150 часов.
- **3-я итерация**: Главы 9-13 (50 уровней) = 150 часов.
- **4-я итерация**: Главы 14-15 (20 уровней) + Финал + Prestige = 100 часов.

Каждая итерация — сама по себе законченный продукт, можно релизить.

---

## 21. Педагогическое обоснование ключевых решений

- **Почему аркада, а не курс.** Ученики бросают курсы через 2 недели (стат. Coursera 4-6%). Мобильные игры удерживают 30-40% на месяц. Играя, ученик наращивает скилл незаметно.
- **Почему мгновенная обратная связь.** Классическое B.F. Skinner + современное «operant conditioning». Ошибка через 5 секунд закрепляется в разы хуже, чем через 0.1 секунды.
- **Почему интервальное повторение.** Кривая забывания Эббингауза: без повторения через 24 часа теряется 70% информации. С повторением через 1/2/7/30 дней теряется 5%.
- **Почему рекурсия визуализируется матрёшкой.** Абстрактное понятие → конкретный образ = детский мозг понимает мгновенно.
- **Почему нет «провала уровня».** Игры без штрафов увеличивают retention на 20-30% (данные Duolingo). Игрок должен всегда чувствовать прогресс.
- **Почему конструктор кода.** Собрать из блоков в 5-10 раз быстрее, чем печатать. При этом синтаксис усваивается моторной памятью.
- **Почему Профик персонализирует ошибки.** Голос «доброго учителя» снижает страх ошибки на 40% (исследования Khan Academy).

---

## 22. Открытые вопросы для обсуждения

1. **Одноязычность (только русский)** vs двуязычность (EN как секретка) — что важнее?
2. **Соревновательный элемент** в Python-режиме (лидерборд по времени прохождения глав) — вводить или нет?
3. **Групповое прохождение** (класс идёт вместе, учитель видит прогресс) — приоритет высокий?
4. **Экспорт кода** — после уровня можно скачать собранный код в файл `.py` для практики в PyCharm/Wing?
5. **Финальный сертификат** — реальный (с QR + номером, проверяемый) или только виртуальный?
6. **Prestige** — сразу в MVP или в v2?
7. **Ежедневный «Разогрев»** — обязательный или опциональный?

---

**Готов к обсуждению и корректировкам.** Документ рассчитан на итеративное уточнение с учителем, гейм-дизайнером и учениками первой волны.
